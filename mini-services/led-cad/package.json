{
  "name": "led-cad-service",
  "version": "0.1.0",
  "private": true,
  "description": "LED STRUCTURE CAD — mini-CAD paramétrico Python/FastAPI (processo único, porta 3000)",
  "scripts": {
    "dev": "py -m uvicorn main:app --host 127.0.0.1 --port 3000 --reload",
    "start": "py -m uvicorn main:app --host 127.0.0.1 --port 3000"
  }
}
