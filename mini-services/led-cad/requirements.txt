# LED Structure CAD - manifesto de dependencias (F0, 19/09/2026)
# Ambiente validado: Windows 11, CPython 3.12.10 (MSC v.1943 64 bit AMD64).
# Instalacao reproduzivel:
#   py -m pip install --user -r requirements.txt
# Frontend e arquivo unico (static/index.html, three.js local). Sem outras deps.
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.12.2
# Apenas para TestClient (teste A09): <0.28 por compatibilidade com
# starlette 0.27 empacotada no fastapi 0.104.1.
httpx<0.28

# COMANDOS (modo direto):
#   cd mini-services/led-cad
#   py -m uvicorn main:app --host 127.0.0.1 --port 3000   (ou 3100)
# Modo shell Next: 3000 encaminha para FastAPI 3100 (nao usar ambas).
# Testes (nao tocam data/_session.json real; usam diretorio temporario):
#   py -B tests/test_groq_contract.py
#   py -B tests/test_f0_repro.py
