# Worklog — LED STRUCTURE CAD

---
Task ID: 1
Agent: Z.ai Code (principal)
Task: Implementar "LED STRUCTURE CAD" — mini-CAD paramétrico PYTHON-FIRST para estruturas de painéis de LED (especificação completa em /home/z/my-project/upload/prompt programa.txt; referência visual em PAINEL 4x2 OUTDOOR-PILAR 3m-ORÇAMENTO.pdf).

Work Log:
- Lida a especificação completa (38 seções) e estudado o PDF de referência (prancha LED EXPERT 222-2023-11-FL01/FL02: painel 4x2m, poste 3m, METALON 60x60x2/40x40x2, chapa de base 680x680x25, chumbadores Ø1", selo "PROJETO SOMENTE PARA ORÇAMENTO").
- Verificado ambiente: Python 3.12.14 + FastAPI 0.128 + Pydantic 2.12 + reportlab 4.4.9 + ezdxf 1.4.3 já disponíveis; gateway Caddy encaminha `?XTransformPort=N` (Caddyfile :81).
- FASE 1 (motor geométrico): `mini-services/led-cad/` com models/ (Element/ProjectModel Pydantic, catálogo de 16 perfis com kg/m), core/geometry.py (vetores/AABB mm), services/preset_service.py (PresetBuilder interpreta presets/dooh/*.json e gera quadro, verticais, horizontais, travessas, diagonais zigzag/X, poste, chapas, chumbadores com IDs estáveis POSTE-01, V01, VB01, HS01, TS01, DI01, BASE-01, CH01…), core/operations.py (move/set_position/add_beam/add_post/add_plate/duplicate/delete/update_profile/rename + undo 50 níveis), core/bom.py (agrupa perfis/chapas/chumbadores com pesos).
- FASE 5-6 (vistas + cotas): drawing/projections.py (projeções ortográficas reais FRONTAL/TRASEIRA/LATERAL/SUPERIOR/INFERIOR/ISOMETRICA; barras como contorno CAD com linha de centro; rótulos de perfil com linha de chamada), drawing/dimensions.py (cotas 100% derivadas da geometria: modularização de vãos, vão total, pé-direito, altura do painel, profundidade), drawing/svg.py (render SVG vetorial compartilhado).
- FASE 7 (prancha): export/pdf.py (reportlab A2 paisagem: FRONTAL grande + LATERAL + PERSPECTIVA em cima, SUPERIOR + INFERIOR no meio, LISTA DE MATERIAIS completa + PESO TOTAL GERAL embaixo, coluna direita com NOTAS, revisões, logo LED COLLOR, selo vermelho "PROJETO SOMENTE PARA ORÇAMENTO / NÃO UTILIZAR PARA FABRICAÇÃO", legenda OBRA/CONTÉM/Data/Desenhista/Nº/FORMATO A2/REVISÃO); drawing/dxf.py (DXF R2010 1:1 mm com camadas).
- FASE 8 (IA): services/ai_service.py — parser determinístico PT-BR (regex) que converte linguagem natural em OPERAÇÕES estruturadas; nunca desenha geometria. Entende: "adicione outro poste 1200 mm para a direita", "mova o poste central 400 mm para esquerda", "duplique V01 500 para a direita", "exclua V03", "mude o perfil de V01 para METALON_80x80x3", "painel 6x3" (m→mm automático), "largura 5000", "pé direito 3500", "preset 2 postes", "desfaça".
- API FastAPI (main.py, porta 3100): /api/health, /api/meta, /api/presets, /api/project[/new], /api/operations, /api/undo, /api/bom, /api/views[/ {name}], /api/export/{pdf,dxf,json}, /api/ai/command. Static servido pelo próprio FastAPI.
- Frontend single-file (static/index.html): HTML/CSS/JS vanilla inline + Three.js 0.160 vendado localmente (import map dinâmico que preserva XTransformPort do gateway). Layout: ESQUERDA (presets, dimensões do painel, árvore de elementos agrupada) | CENTRO (editor 3D com orbit/zoom/pan, botões de vista, grade, gizmo de mover TransformControls, strip de vistas 2D SVG com abas) | DIREITA (propriedades: perfil, start/end numéricos, duplicar/excluir) | RODAPÉ (barra IA + Materiais/Undo/DXF/JSON/GERAR PDF). Toasts, modal BOM, loading, responsivo.
- Next.js page.tsx reduzido a shell de preview: iframe `/?XTransformPort=3100` (nenhuma lógica de CAD no Node — arquitetura Python-first preservada).
- Correções durante E2E (agent-browser): bbox da câmera incluía o solo 40m (cena "vazia") → agora enquadra só a estrutura; duplicação de PAINEL-LED na árvore; "painel 6x3" interpretado como mm → heurística m/mm; inputs não sincronizavam após regeneração da IA; DXF write para StringIO; layout do PDF reorganizado em faixas sem colisão.
- Verificação E2E completa (agent-browser via http://localhost:81/ — caminho real do preview): carregamento, seleção por árvore E por clique 3D (raycast confirmado), edição numérica aplicada (HI01 end.x 2000→-1500→undo), IA (add poste, mova, painel 6x3, pé direito 3500, desfaça), BOM modal, abas de vistas SVG, presets 1/2 postes, export PDF 200/attachment, DXF 145KB, JSON, mobile 390px OK, lint do projeto limpo, 0 erros no server.log (únicos erros de console eram artefatos de eventos sintéticos de teste).

Stage Summary:
- Serviço Python estável na porta 3100 (`bun run dev` em mini-services/led-cad, uvicorn --reload).
- App acessível no preview: `/` (shell Next) → iframe → gateway `?XTransformPort=3100` → FastAPI.
- Prancha PDF vetorial A2 fiel ao layout de referência, BOM automático (507,53 kg no preset 4x2), cotas sempre derivadas da geometria.
- Arquivos-chave: mini-services/led-cad/{main.py, core/, models/, services/, drawing/, export/, presets/, static/index.html}; src/app/page.tsx (shell).
- Próximos passos sugeridos: snap ao criar barras, presets parede/suspenso/rental, chapa expandidida na vista inferior, persistência de projetos em data/*.json, GLTF export, histórico de operações mais rico.

---
Task ID: 2 (cron review rodada 1)
Agent: Z.ai Code (webDevReview automático)
Task: QA completo + correção de bug + novas features (snap/seção 17, persistência/seção 27) + polimento de UI.

Work Log:
- QA via agent-browser: detectado BUG no parser de IA — comando "preset 2 postes" (prometido no texto de ajuda) não era reconhecido (só casava com o ID completo DOOH_4X2_2_POSTES). Corrigido em services/ai_service.py: reconhece "preset N postes" escolhendo o preset com defaults.posts==N, ou regenera o atual com N postes; "preset" sem número lista os disponíveis. Verificado: posts=2 após comando.
- BUG de sintaxe introduzido durante refactor do pointerup (fechamento `});` órfão) derrubava o módulo JS — detectado via console e validador node, corrigido; adicionado try/catch com toast no clique 3D para nunca falhar em silêncio.
- FEATURE snap em Python (seção 17): core/snap.py com prioridades extremidades (250mm) > centros (180mm) > grade (50mm), exposto em POST /api/snap — a lógica de snap é do core, o Three.js só desenha o marcador ( PYTHON-FIRST preservado).
- FEATURE modo "Barra A→B": cliques no 3D capturam ponto (mesh ou plano do solo), consultam /api/snap, mostram marcador colorido por regra (laranja=endpoint, azul=center, cinza=grid), linha tracejada de preview em tempo real e criação via add_beam com encadeamento (B vira o próximo A). Tecla B.
- FEATURE régua: dois cliques → Δ total + componentes X/Y/Z em chip flutuante (Δ 10.262,2 mm verificado). Tecla R.
- FEATURE persistência (seção 27): services/project_service.py + rotas /api/projects (list/save/load/delete) gravando em data/*.json; UI "PROJETOS SALVOS" no painel esquerdo com nome, contagem de elementos, dimensões, data, carregar (↥) e excluir (✕). Testado save/load round-trip (Painel QA 4x2 restaurado: 4000x2000, 1 poste, 51 elementos).
- FEATURE export BOM em CSV (/api/export/bom.csv, separador ";" e BOM UTF-8 pronto para Excel).
- STYLE: toolbar reorganizada com separadores (Selecionar/Barra/Régua | vistas | toggles), banner de modo, chip de medição, legendas de cores dos perfis na árvore, badges de comprimento mm nos itens da árvore, hover com elevação em botões/chips, hover highlight 3D (emissive) nos meshes, tooltips com atalhos (B/R/G/F/Del/Esc/Ctrl+Z/Ctrl+S), atalhos de teclado implementados (B barra, R régua, G gizmo, F enquadrar, Esc cancela modos, Ctrl+Z undo, Ctrl+S salvar).
- Verificação final: PDF 200, CSV 200, DXF 200, projetos 200, server.log sem erros, console limpo em aba nova (erros setPointerCapture listados pelo agent-browser são artefatos acumulados de eventos sintéticos de teste, não ocorrem com ponteiro real).

Stage Summary:
- Serviço saudável na 3100; app completo via preview (iframe gateway).
- Novas capacidades: criação de barras com snap Python no 3D, régua, projetos salvos em JSON, CSV do BOM, modos de ferramenta com atalhos.
- Arquivos novos/alterados: core/snap.py, services/project_service.py, services/ai_service.py (fix preset), main.py (rotas novas), static/index.html (modos, projetos, estilo), data/painel-qa-4x2.json (exemplo salvo).
- Próximos passos sugeridos: presets parede/suspenso/rental, chapa expandida na vista inferior (hachura), snap em interseções/planos verticais, histórico de operações (log lateral), zoom to selection na árvore, GLTF export.

---
Task ID: 3 (cron review rodada 2)
Agent: Z.ai Code (webDevReview automático)
Task: QA completo via agent-browser + correção de bugs (undo pós-regeneração, Installation posts=None, chapa de parede irreal, pricing.json listado como projeto) + novas features (presets PAREDE/SUSPENSO, orçamento em R$ editável, histórico de operações, export GLTF, zoom à seleção, vista expandida) + polimento de UI.

Work Log:
- QA inicial: /api/health OK (51 elems), exports 200 (pdf/dxf/json/csv), app carregando via gateway (:81 → 3100), 3D/vistas/presets OK. Encontrados 4 bugs reais (abaixo), todos corrigidos e re-testados.
- BUG 1 (crítico): regenerações ("largura X", "painel AxB", "Aplicar ao projeto") limpavam a pilha de undo → "desfaça" dizia "Nada para desfazer". Fix: ProjectStore.new_from_preset(keep_undo=True) faz push_undo ANTES de regenerar; nova rota POST /api/project/regen (mantém undo, ao contrário de /new que troca de projeto); IA __regen usa keep_undo; botão "Aplicar" migrou para /regen. Verificado: largura 5000 → Ctrl+Z → 4000 (no preset e no botão).
- BUG 2: preset parede tem posts=0; select do frontend não tinha opção 0 → parseInt("")=NaN → JSON null → ValidationError (posts int). Fix duplo: validator _posts_none_ok em Installation (None/NaN → 0) + option "0 — sem postes (parede)" e parseInt(...)||0 no frontend.
- BUG 3: chapa de apoio da parede era 220x2000x10 (57kg cada → R$ 3.674 irreal). Redesenhado: 15 chapas 220x220x10 (uma por braçada, 3 níveis × 5 verticais) = 57 kg total, orçamento realista (R$ 5.5k).
- BUG 4: data/pricing.json aparecia na lista de projetos salvos ("0 elems"). Fix: list_projects ignora arquivos sem model.elements.
- Fix extra: _unique_id agora preserva o padrão do ID original (V01 → V11, antes gerava "V-01").
- FEATURE presets PAREDE e SUSPENSO (arquitetura Python-first): PresetBuilder._build_supports() ramifica por installation.type — wall: 15 braçadas METALON_50x50x2 (BR01..) + 15 chapas de apoio (WP01..) no grupo FIXAÇÃO PAREDE, sem postes/bases; suspended: 2 tirantes TUBO_76x3.2 (TIR-01/02) + chapas de ancoragem (ANC-01/02) + viga de teto METALON_100x100x3 no grupo SUSPENSÃO. Presets: presets/parede/4x2_parede.json (WALL_4X2_PAREDE, PD 2500, depth 600) e presets/suspenso/4x2_suspenso.json (SUS_4X2_SUSPENSO, drop 1200, indoor). Testados: 64/49 elementos, render 3D correto.
- FEATURE orçamento em R$: services/pricing_service.py (preços editáveis persistidos em data/pricing.json: perfil R$/kg, chapa R$/kg, pintura R$/kg, perda %, chumbador por Ø); BOM retorna custo_total por item + total_cost_brl + bloco pricing; modal BOM com coluna "Custo (R$)" destacada, barra de preços editável + "↻ Recalcular" (PUT /api/pricing → re-render; verificado 5.189 → 6.549 → 5.189); CSV com coluna CUSTO TOTAL + linha de referência; PDF com "CUSTO ESTIMADO (MATERIAL)" em verde + REF de preços + coluna na tabela (larguras reajustadas: 424mm, não invade a coluna de notas); stats do viewport mostram "R$ X"; IA responde "quanto custa?" com o resumo do orçamento.
- FEATURE histórico de operações (100% Python): ProjectStore.history (120 entradas: time/action/detail) registrado em apply_operation, undo, regen, novo, save, load; GET /api/history; painel "HISTÓRICO" na coluna direita (ícones por tipo, cores para undo/delete/add, refresh a cada adopt); IA "histórico" retorna as 8 últimas ações.
- FEATURE export GLTF 2.0 em Python puro (export/gltf.py): malhas de prisma (quadro 4 lados, redondo 12 lados, chumbador 8 lados), caixas para chapas/painel, conversão mm→m Z-up→Y-up, buffer base64 embutido, 2 materiais PBR; GET /api/export/gltf + botão "GLTF" (200, ~25-28KB).
- FEATURE zoom à seleção: tecla F e botão ⤢ enquadam o elemento selecionado (Box3 do mesh); modal de vista expandida (botão ⤢ nas abas ou duplo clique no SVG) com downloads DXF/PDF no cabeçalho.
- STYLE: badges de categoria nos presets (DOOH teal/PAREDE laranja/SUSPENSO roxo), painel de histórico estilizado, coluna de custo com destaque accent, price-bar em grid, hint e placeholders atualizados, modal de vista 1100px.
- E2E agent-browser: presets parede/suspenso (render + toast + card ativo), BOM com custo e recálculo de preços, IA (quanto custa/preset parede/largura/desfaça/histórico), Aplicar+Ctrl+Z, delete HS01+undo, modos barra/régua (banner), propriedades de BR01 editáveis, load de projeto salvo, exports 200 pelo gateway, mobile 390px empilhado OK, lint limpo, server.log sem erros novos (1 traceback antigo pré-fix documentado).

Stage Summary:
- Serviço Python saudável na 3100; app completo via preview (iframe gateway); 4 presets (2 DOOH + parede + suspenso); orçamento R$ editável integrado a BOM/UI/CSV/PDF/IA; histórico de operações persistente em sessão; GLTF export puro Python.
- Arquivos novos: services/pricing_service.py, export/gltf.py, presets/parede/4x2_parede.json, presets/suspenso/4x2_suspenso.json, data/pricing.json, data/demo-parede-4x2.json, data/demo-suspenso-4x2.json.
- Arquivos alterados: core/operations.py (undo keep + history + _unique_id), core/bom.py (custos), services/preset_service.py (_build_supports: wall/suspended), services/ai_service.py (regen keep_undo, custo/histórico/preset por tipo, HELP_TEXT), services/project_service.py (filtro), models/element.py (validator posts), main.py (/regen, /history, /pricing, /export/gltf, CSV/PDF custo), export/pdf.py (custo + larguras), static/index.html (histórico, orçamento, badges, GLTF, vista expandida, zoom seleção, select postes 0).
- Próximos passos sugeridos: preset RENTAL/palco (installation.type já suporta), snap em planos verticais/interseções, chapa expandida desenhada na vista inferior (hachura), multi-seleção com Shift, gallery de projetos com thumbnail (SVG da vista frontal), import de CSV de preços, undo em cascata para regen (restaurar também preset_id anterior).

---
Task ID: 4 (cron review rodada 3)
Agent: Z.ai Code (webDevReview automático)
Task: QA completo via agent-browser + correção de bugs (layout com scroll, exclusão de projeto sem confirmação) + novas features (preset RENTAL/palco com lastro de concreto, thumbnails de projetos, card de resumo do projeto, modal de ajuda/atalhos) + polimento de estilo.

Work Log:
- QA inicial: health OK (serviço 3100), 6 vistas 200, exports pdf/dxf/json/csv/gltf 200, IA respondendo, BOM com orçamento R$ OK, modais OK. Documentados 2 problemas reais:
- BUG 1 (layout): #app com grid-template-rows "1fr auto" sofria blowout — linha 1 crescia para o min-content (880px) numa janela de 800px → documento rolava 144px e o rodapé de exportação ficava fora da vista. Fix: grid-template-rows: minmax(0,1fr) auto + overflow:hidden no #app + min-height:0 na col-center + view-holder com max-height:min(240px,30vh). Verificado: docH 800 = winH 800, rodapé sempre visível, sem scroll de página.
- BUG 2 (dados): excluir projeto salvo era destrutivo com 1 clique (durante o próprio QA um demo foi apagado por engano — restaurado). Fix: exclusão em 2 passos no frontend — 1º clique arma o botão (fica vermelho "certo?"), 2º clique confirma; desarma sozinho em 3,2s. Backend passa a registrar "delete_projeto" no histórico. Testado: armar → desarmar por timeout → confirmar exclusão.
- BUG 3 (realismo do primeiro desenho do rental): chapa de distribuição 700x1160x10 sólida pesava 64kg (irreal) e a torre ficava entre 2 blocos separados. Redesenhado em Python: 1 bloco de lastro 1000x800x150 sob cada torre + chapa 400x400x12 (15kg) + bloco traseiro 800x500x150 no pé da escora.
- FEATURE preset RENTAL/PALCO (arquitetura Python-first): presets/rental/4x2_rental.json (RENT_4X2_PALCO: torres METALON_100x100x3, escoras 50x50x2, PD 800, indoor) + PresetBuilder._build_rental() — TORRE-nn, DIST-nn (distribuição), PLACA-nn (ligação), ESC-nn (escora contra tombamento), LST-nn/LBR-nn (lastro, grupo LASTRO). 56 elementos, 1.142,92 kg (864 kg concreto + 278 kg aço), R$ 5.719.
- FEATURE material CONCRETO no BOM/pricing: core/bom.py detecta "LASTRO|CONCRETO" no label → densidade 2400 kg/m³ (vs aço 7850), kind "concreto", sem pintura nem perda de corte; pricing_service com concrete_brl_kg (default R$ 0,22/kg, editável via PUT /api/pricing e PricingIn); modal BOM ganhou coluna TIPO com badges coloridos (perfil teal / chapa indigo / chumbador âmbar / concreto rosa), linhas de concreto destacadas e input "Concreto (R$/kg)" (aparece só quando há concreto). CSV também reflete.
- FEATURE thumbnails de projetos (Python-first): project_service gera data/thumbs/{pid}.svg (vista frontal via project_view+render_svg 320x200) no save; list_projects retorna flag thumb; rota GET /api/projects/{pid}/thumb.svg; delete remove o thumb; frontend mostra mini-SVG 46x30 no item da lista (img com lazy + XTransformPort). Projetos antigos retroalimentados re-salvando.
- FEATURE card de resumo do projeto: painel PROPRIEDADES sem seleção agora mostra nome, badge do preset, grid com Painel/Profundidade/Pé direito/Instalação/Elementos/Peso, custo estimado em destaque e atalhos "Materiais" e "PDF" (testados).
- FEATURE modal de ajuda: botão "?" na toolbar + tecla "?" abrem modal com atalhos de teclado (kbd), interação 3D e lista viva dos comandos de IA (busca /api/meta); Esc fecha todos os modais agora.
- IA: "preset rental | palco | evento | torre | stage" carrega o RENT_4X2_PALCO; keywords outdoor/dooh/pilar/poste para o DOOH; HELP_TEXT e mensagem de erro atualizados.
- STYLE: badge RENTAL (rosa #be185d), blocos de concreto com material fosco bege no 3D (distinto do aço), .proj-thumb, kbd/.help-*, kind-concreto na tabela BOM.
- E2E final (agent-browser via :81): 5 presets na lista, rental carrega e renderiza, troca de perfil TORRE-01 → METALON_120x120x3 atualizou peso/custo (1.142,92→1.144,12 kg), Ctrl+Z undo OK, IA criou POSTE-01 em X=600 + undo, save pela UI gera thumb, abas de vista OK, modal de ajuda OK, mobile 390px empilhado OK, lint limpo, 0 erros novos no server.log (único traceback é pré-existente de rodada anterior, antes do fix do posts=None).

Stage Summary:
- Serviço Python saudável na 3100; app 100% em viewport (rodapé fixo); 5 presets (2 DOOH + PAREDE + SUSPENSO + RENTAL); BOM com 3 materiais precificados (aço perfil, chapa, concreto); galeria de projetos com miniaturas vetoriais geradas pelo motor de desenho Python.
- Arquivos novos: presets/rental/4x2_rental.json, data/thumbs/*.svg (3+), data/rental-demo-4x2.json.
- Arquivos alterados: static/index.html (layout minmax, delete 2 passos, thumbs, resumo, ajuda, badges, concreto 3D), services/preset_service.py (_build_rental), services/project_service.py (thumbs), services/pricing_service.py (concrete_brl_kg), core/bom.py (concreto + kind), services/ai_service.py (rental keywords), main.py (thumb route, delete log, PricingIn, thumbnail no save), models/element.py (sem mudança — rental já estava no Literal).
- Próximos passos sugeridos: multi-seleção com Shift + operações em lote, snap em planos verticais/interseções, chapa expandida desenhada na vista inferior (hachura), import de CSV de preços, undo em cascata no regen (restaurar preset anterior), PDF do rental mencionando concreto nas notas.

---
Task ID: 5 (cron review rodada 4)
Agent: Z.ai Code (webDevReview automático)
Task: QA completo via agent-browser + features novas (multi-seleção Shift com operações em lote, snap em arestas, import de CSV de preços, IA em lote) + 2 bugs corrigidos (regex "todos os", perfil IA nunca casava) + polimento de UI.

Work Log:
- QA inicial: health OK, 5 presets, árvore/seleção/propriedades, IA add poste + undo, BOM com custo/tipo, abas de vista, modal expandido (falso-positivo descartado: estilo inline era artefato do próprio script de teste), exports pdf/dxf/csv/gltf 200, mobile 390px empilhado OK, console limpo. Fase estável → seguiu para features.
- DETECTADO usuário real interagindo via preview (requests 21.0.0.1 no server.log: presets carregados, snap, BOM) — testes destrutivos passaram a ser feitos em store isolado (Python direto) para não poluir a sessão.
- FEATURE multi-seleção + lote (100% Python no core): core/operations.py com multi_delete/multi_move/multi_duplicate/multi_profile — cada lote = 1 snapshot de undo (Ctrl+Z desfaz tudo de uma vez) e 1 linha no histórico; _ids_from_op normaliza lista; Batch: skipped IDs inexistentes não abortam o lote.
- FEATURE UI multi-seleção: Shift+clique no 3D e na árvore alterna elementos no lote (teal #14b8a6); chip "N selecionados" no viewport; painel PROPRIEDADES vira painel de lote (contagem, peso aproximado do lote, chips de IDs removíveis, mover ΔX/ΔY/ΔZ, trocar perfil em massa (só se todos forem barras), duplicar lote +300mm X, excluir lote com confirmação em 2 passos); Del exclui o lote; Esc limpa; adopt() limpa o lote em regen/undo; hover não rouba mais a cor de elementos multi-selecionados; applyOp repassa element_ids.
- FEATURE snap em ARESTAS (core/snap.py): _project_on_segment projeta o ponto no eixo da barra (clamp 0..1), raio EDGE_MM=120, prioridade endpoint > center > edge > grid; retorna rule "edge"; frontend pinta o marcador de roxo (0x8b5cf6) e o banner do modo Barra explica a legenda de cores. Testado: ponto 100mm fora do eixo → projeta exato na barra.
- FEATURE import de CSV de preços: services/pricing_service.import_pricing_csv (aliases perfil/chapa/pintura/concreto/perda/chumbador_NN, aceita ; ou ,, vírgula decimal, R$, ignora linhas inválidas listando o motivo) + rota POST /api/pricing/import-csv + botão "⤒ Importar CSV" na barra de preços do BOM (FileReader → API → BOM recalculado). Testado: 4 preços aplicados, lixo ignorado.
- FEATURE IA em lote: "exclua V01 e V02" (vários IDs no texto), "exclua todos os postes/diagonais/chapas…" e "mova todos os postes 200 mm para a direita" (por papel), "mude o perfil de todos os verticais para …"; _find_ids_multi coleta TODOS os IDs citados; _ids_by_role resolve papéis (ROLE_KEYWORDS); HELP_TEXT atualizado.
- BUG 1 (novo, introduzido e corrigido na mesma rodada): regex \btodos?(os|as)\b nunca casava com "todos os" (espaço) → \b(todos?|todas?)\b.
- BUG 2 (PRÉ-EXISTENTE descoberto): parser de perfil usava t.upper() + regex com 'x' minúscula → "mude o perfil de V01 para METALON_80x80x3" (exemplo da própria ajuda!) SEMPRE falhava. Fix: busca case-insensitive + canonicalização pelo catálogo (METALON_80X80X3 → METALON_80x80x3).
- FIX extra: hook de debug window.__cad.select agora repassa o flag additive.
- STYLE: .tree-item.msel (teal), chip .msel-count, .batch-* (head gradiente, chips removíveis com hover vermelho, separadores, hint), botão Importar CSV na price-bar, histórico com ícones/cores para multi_*, ajuda com linha Shift+clique.
- E2E final: multi-seleção 3 elementos (chip + painel + 3 .msel na árvore) → mover +150mm (1 histórico, 1 undo) → perfil lote METALON_60x60x2 (verificado no modelo) → excluir lote com arma/confirma (49 elems) → toggle add/remove → Esc limpa → IA em lote 7/7 testes em store isolado → BOM/CSV/help OK → lint limpo, 0 erros no server.log.

Stage Summary:
- Serviço Python saudável na 3100; app estável com usuário real ativo no preview.
- Novas capacidades: seleção múltipla Shift+clique com 4 operações em lote (undo único), snap em arestas com legenda de cores, importação de CSV de preços, IA em lote por IDs/papéis, parser de perfil corrigido.
- Arquivos alterados: core/operations.py (4 multi_*), core/snap.py (edge), services/pricing_service.py (CSV), services/ai_service.py (lote + 2 fixes de regex), main.py (import-csv, want_edges), static/index.html (multi-seleção completa, painel de lote, cores snap, CSV, ajuda).
- Próximos passos sugeridos: seleção por retângulo (marquee), "selecionar todos do grupo" no cabeçalho do grupo da árvore, desfazer em cascata no regen, chapa expandida com hachura na vista inferior, comparador de versões de projeto (diff de BOM entre salvamentos).

---
Task ID: 6 (cron review rodada 5)
Agent: Z.ai Code (webDevReview automático)
Task: QA completo via agent-browser + novas features (vento NBR-simplificado, plano de corte FFD, seleção por retângulo marquee com matemática em Python, seleção de grupo, persistência de sessão) + 1 bug corrigido (List faltando no MarqueeIn) + hachura de chapas nas vistas de plano + polimento de UI.

Work Log:
- QA inicial: health OK, 5 presets, todos os endpoints/exports 200, app carregando via gateway (:81 → 3100). Traceback antigo de regen (posts=None) confirmado COMO STALE: validador _posts_none_ok cobre o caminho PresetBuilder (testado isolado em Python: Installation(posts=None)→0, regen parede→parede OK). Undo em cascata do regen também restaurando preset_id (verificado). Sessão REAL do usuário observada (SUSPENSO regenerado 1920x960x600, 35 elems, 132,57 kg — exclusão HM01 às 21:23) — QA em modo somente-leitura na sessão ao vivo.
- BUG corrigido (detectado no E2E do marquee): POST /api/select/marquee → 500 "MarqueeIn not fully defined" — faltava importar List do typing em main.py (usado em Optional[List[Dict[str,float]]]). Fix 1 linha + re-teste OK.
- MELHORIA de robustez motivada pelo reload do uvicorn durante o deploy (a sessão aberta do usuário foi perdida): ProjectStore agora PERSISTE a sessão em data/_session.json após cada operação/undo/regen/load (best-effort, nunca derruba a API) e RESTAURA no primeiro ensure_loaded() após restart. Verificado: ciclo completo preset→op→nova instância→restaurado; histórico registra "Sessão anterior restaurada (data) — N elementos"; o reload real do servidor restaurou a sessão default.
- FEATURE vento (core/wind.py, 100% Python, raciocínio NBR 6123 simplificado p/ orçamento): q=0,613·V₀² (V₀ default 35 m/s editável), Ce 1,2 painel / 1,3 treliça; área da treliça por PROJEÇÃO REAL das barras no plano frontal (barras engrossadas pela largura do perfil, sobreposições frente/fundo contam 1× por faixa de 50 mm; treliça na sombra do painel conta 30%). Efeitos por instalação: post → corte na base + kN/poste + momento de tombamento; wall → kN por fixação (conta chapas WP); suspended → tração por tirante; rental → FS de tombamento (momento vento × lastro·g·braço) com alerta <1,5. Rotas: GET /api/wind?v0=; bom_full() anexa "wind" a TODAS as respostas com BOM (project/ops/undo/regen/ai/pricing). PDF: bloco "VENTO ESTIMADO" nas NOTAS + linha de instalação específica por tipo (outdoor/parede/teto/rental).
- FEATURE plano de corte (core/cutlist.py): agrupa barras por perfil+comprimento (ceil p/ mm inteiro), empacota em barras comerciais de 6.000 mm via First-Fit-Decreasing com perda de serra 3 mm, calcula sobra % e plano por barra. Rotas: GET /api/cutlist + GET /api/export/cutlist.csv (Excel ; + BOM UTF-8).
- FEATURE seleção por retângulo (core/select.py, Python-first): frontend envia origem (câmera) + 4 direções de raio dos cantos; Python monta o tronco de pirâmide (cross de raios vizinhos, normais para dentro) e testa AABB de cada elemento contra as 4 semi-faces (+ teste "à frente da câmera"); fallback planar right/up p/ câmera orto. Rota POST /api/select/marquee. Testado isolado: painel inteiro=42, retângulo pequeno no poste=[POSTE-01] exato, céu=0.
- FEATURE IA (services/ai_service.py): "vento?" → resumo F/áreas/efeito; "plano de corte" → resumo por perfil + total de barras; "selecione todos os <papel>" / "selecione tudo" / "limpe a seleção" → retorna result.selection (lista de IDs) que o frontend aplica na multi-seleção (selectMany) ou limpa; HELP_TEXT atualizado.
- FEATURE desenho: chapas agora são desenhadas como RETÂNGULO fechado + cruz de eixos (antes era só 1 diagonal!) + HACHURA 45° nas vistas SUPERIOR/INFERIOR (SVG/DXF/PDF: layer "hachura" #94a3b8, DXF mapeado p/ CHAPAS); chumbadores vistos de ponta (vistas de plano) viram círculo + cruz de centro. Verificado: 30 linhas de hachura na VISTA INFERIOR do DOOH (chapa de base 680x680).
- FRONTEND: modo "⬚ Área" (chip na toolbar + tecla M) com elástico (div .marquee-rect) e controles de órbita desativados durante o arrasto; clique curto cai na seleção normal; selectMany(ids, additive) para marquee/grupo/IA; botão "todos" no cabeçalho de cada grupo da árvore (seleciona o grupo inteiro); modal BOM reorganizado em 3 ABAS: MATERIAIS & ORÇAMENTO (como antes), PLANO DE CORTE (cards por perfil com chips pçs/metros/barras/sobra + visualização das barras com segmentos coloridos proporcionais + CSV), VENTO (hero stats F/áreas/centro de pressão + input V₀ + Recalcular + bloco de efeito + nota); card de resumo ganhou chip âmbar de vento (clique abre a aba Vento); sendAi aplica result.selection; HISTÓRICO agora preenche a altura restante da coluna direita (flex); ajuda com M/modo área/"todos" do grupo; placeholder da IA com os novos comandos.
- E2E via agent-browser: marquee cheio (50 elems → chip + 50 itens teal + painel de lote), marquee parcial (6 elems — substitui a seleção), lote movido +250X (POSTE-01+BASE+CH011..) e Ctrl+Z desfez; IA selecionou 10 verticais, limpou, "todos" do grupo selecionou 15; vento recalculado V₀ 35→45 → F 7,88→13,18 kN (escala V² correta); abas do BOM (3 grupos de corte, 17 barras, 61 segmentos); hachura na faixa de vistas; exports pdf/dxf/csv/gltf/cutlist.csv 200; sessão restaurada após reload real; mobile 390px sem scroll de página; lint limpo; únicos erros de console = artefatos setPointerCapture de eventos sintéticos (confirmado com contador de erros por mensagem).

Stage Summary:
- Serviço Python saudável na 3100; app estável; sessão agora sobrevive a restarts do servidor.
- Novas capacidades: estimativa de vento integrada (API/BOM/UI/PDF/IA/CSV... bom, PDF+UI+IA), plano de corte FFD para serralheria (aba + CSV), seleção por retângulo com matemática exata em Python, seleção de grupos, chapas com hachura técnica nas vistas de plano.
- Arquivos novos: core/wind.py, core/cutlist.py, core/select.py, data/_session.json.
- Arquivos alterados: main.py (bom_full, /api/wind, /api/cutlist, /api/select/marquee, /api/export/cutlist.csv, import List), core/operations.py (_persist/_restore_session), services/ai_service.py (vento/corte/seleção/HELP_TEXT), drawing/projections.py (_draw_plate + hachura + bolt circle + LAYERS), drawing/dxf.py (hachura→CHAPAS), export/pdf.py (bloco vento + linha de instalação por tipo), static/index.html (modo Área, selectMany, abas do BOM, corte/vento UI, chip de vento no resumo, histórico flex, ajuda).
- Próximos passos sugeridos: comparador de versões (diff de BOM entre salvamentos), presets personalizados editáveis na UI (salvar configuração atual como preset do usuário), import de projeto JSON, escolha do comprimento de barra comercial (6/8/12 m) no plano de corte, direção do vento selecionável (frontal/lateral/traseira), DXF por vista em arquivos separados.

---
Task ID: 7 (cron review rodada 6)
Agent: Z.ai Code (webDevReview automático)
Task: QA completo via agent-browser + 4 features novas (comparador de versões com diff de BOM, presets pessoais snapshot, barras comerciais 4/6/8/12 m no plano de corte, direção do vento frontal/traseira/lateral) + polimento de estilo/ajuda.

Work Log:
- QA inicial: health OK (51 elems, sessão DOOH restaurada), 5 presets, exports 200, abas do BOM OK, 2 × 500 no server.log confirmados STALE (bugs já corrigidos em rodadas anteriores — regen posts=None e MarqueeIn List). Console sem erros novos.
- INCIDENTE corrigido durante o QA: meu teste isolado inicial rodou ANTES de redirecionar SESSION_FILE e _persist() sobrescreveu data/_session.json com V01=METALON_120x120x3; o reload do uvicorn (por causa das edições seguintes) restaurou essa sessão poluída na store ao vivo. Detectado pelo custo R$ 10.242 no painel; restaurado via operação update_profile V01 → METALON_60x60x2 (507,53 kg / R$ 9.862,16 conferidos). Lição registrada: testes isolados DEVEM redirecionar SESSION_FILE antes de qualquer ProjectStore().
- FEATURE comparador de versões (Python-first): services/project_service.py com bom_snapshot_from() (resumo compacto do BOM: descr/kind/unid/quant/peso/custo + totais) gravado no JSON do projeto a cada save; diff_saved_vs_current() compara snapshot vs BOM atual → added/removed/changed (Δquant/Δpeso/Δcusto, ordenado por impacto) + totais lado a lado; list_projects retorna flag "comparable". Rotas: POST /api/projects/save agora grava o snapshot; GET /api/projects/{pid}/diff. UI: botão ⇄ no item de projeto salvo (só quando comparável) abre modal #diffModal com hero de 4 cards (salvo em / peso→Δ / custo→Δ / itens alterados) e tabela com chips +/− verde/vermelho e valores antigos riscados; projetos antigos sem snapshot mostram mensagem orientando re-salvar.
- FEATURE presets pessoais (snapshot template): services/preset_service.py com save_custom_preset() → presets/custom/{slug}.json com kind="snapshot" + modelo INTEIRO (captura TODAS as edições: barras movidas, perfis, extras; mesmo nome sobrescreve — iteração), delete_custom_preset() (só apaga em custom/), list_presets agora tolera JSON corrompido e marca custom=true. core/operations.new_from_preset detecta kind=snapshot e reconstrói o ProjectModel direto (sem PresetBuilder; overrides ignorados por design) + SEMPRE alinha model.preset_id ao preset pedido (fix: card ativo apontava o preset de origem). Rotas: POST /api/presets/custom {name}, DELETE /api/presets/custom/{pid}. UI: botão ＋ no header PRESETS abre #customPresetModal (nome pré-preenchido), badge CUSTOM violeta #7c3aed, cards custom primeiro na lista, ✕ de exclusão em 2 passos ("certeza?" 3,2s) igual aos projetos.
- FEATURE barras comerciais no plano de corte: core/cutlist.py com STOCK_LENGTHS_MM (4000/6000/8000/12000) + normalize_bar_len() (fora da lista → 6 m); build_cutlist(model, bar_len_mm) empacota FFD no comprimento escolhido; chave "bars" substitui "bars_6m". Rotas: /api/cutlist?bar_len= e /api/export/cutlist.csv?bar_len= (cabeçalho "BARRAS 8M" etc.). UI: controle segmentado 4/6/8/12 m na aba Plano de corte (recalcula na hora, chips "N barras X m", CSV segue a escolha). Verificado: 18→14→10 barras (6→8→12 m) no DOOH.
- FEATURE direção do vento: core/wind.py com normalize_direction() e frame_projected_areas(model, direction) — frontal/traseira projetam X-Z (largura×altura), lateral projeta Y-Z (profundidade×altura: só o perfil da treliça + face dos postes pegam vento); área do painel lateral = profundidade×altura; resposta inclui direction/direction_label e a nota cita a direção. Rota: /api/wind?v0=&dir=. UI: controle segmentado Frontal/Traseira/Lateral na aba Vento (recalcula + toast com direção; card "Força total" ganhou sublinha com a direção). Verificado: DOOH frontal 7,88 kN → lateral 1,97 kN (área 8→1,6 m²).
- IA: "vento lateral | vento traseiro" (por direção), "plano de corte com barras de 8 m" (bar_len), "salve preset <nome>" (cria preset pessoal + log), e "preset <palavras do nome>" agora casa pelo NOME do preset (matching por hits de palavras, roda antes das keywords de instalação) — presets personalizados carregáveis pela IA. sendAi atualiza a lista de presets quando a IA cria um. HELP_TEXT atualizado.
- STYLE: .cat-custom violeta, .preset-del (2 passos), .diff-hero/.diff-chip/.diff-add/.diff-rem/table.diff (antigo riscado vermelho, novo verde), .seg-ctl (controle segmentado dos seletores), modal #diffModal e #customPresetModal, ajuda ganhou seção "🧰 Recursos" (＋ preset pessoal, ⇄ comparar, barras comerciais, direção do vento), placeholder dos chips de corte e vento com direção.
- E2E final (agent-browser via gateway): plano de corte 6/8/12 m (18/14/10 barras), vento frontal→lateral (7,92→1,97 kN) com toast, preset pessoal criado pela UI (badge CUSTOM no topo), snapshot recarregado descarta edição feita depois (poste +500 mm → volta a X=0), preset_id do card ativo correto pós-fix, diff modal com 2 itens (60x60 −1 / 120x120 +1, +R$ 280,32) batendo com os totais, delete 2-passos do preset e do projeto de teste, IA "vento lateral" pela barra, CSV 12 m com header "BARRAS 12M", mobile 390px sem scroll de página, modal de ajuda com Recursos, lint limpo, 0 erros novos no server.log. Artefatos de teste limpos (projeto e2e-diff e preset CUSTOM_* excluídos); sessão final restaurada: DOOH_4X2_1_POSTE, 51 elems, 507,53 kg, R$ 9.862,16.

Stage Summary:
- Serviço Python saudável na 3100; app estável; lint limpo; sessão limpa.
- Novas capacidades: comparador de versões (diff de materiais salvo↔atual em Python), presets pessoais tipo template (snapshot integral do projeto), plano de corte com barra comercial 4/6/8/12 m, vento com direção selecionável (frontal/traseira/lateral).
- Arquivos novos: presets/custom/ (diretório, criado on-demand).
- Arquivos alterados: core/cutlist.py (bar_len + normalize), core/wind.py (direction), core/operations.py (snapshot preset + model.preset_id), services/project_service.py (bom_snapshot_from, diff_saved_vs_current, comparable), services/preset_service.py (custom presets, _slug, listagem tolerante), services/ai_service.py (4 comandos novos + matching por nome + HELP_TEXT), main.py (4 rotas novas/parametrizadas), static/index.html (modais diff/preset, ⇄, ＋, seg-ctl, badges, ajuda Recursos).
- Próximos passos sugeridos: histórico de versões por projeto (v1, v2… em vez de sobrescrever o slug), PDF do orçamento com página de diff, escolha da direção do vento refletida no chip do card de resumo, import de projeto JSON (upload), DXF por vista em arquivos separados, ensaio de vento com fator de obstrução de painéis vizinhos.

---
Task ID: 8 (cron review rodada 7)
Agent: Z.ai Code (webDevReview automático)
Task: QA completo via agent-browser + avaliação de estado → fase ESTÁVEL → 4 features novas (histórico de versões v1/v2…, import de projeto JSON, abrigo de vento por painéis vizinhos, diff A↔B entre salvos) + 1 bug pré-existente corrigido (_session.json listado como projeto) + polimento de UI/estilos.

Work Log:
- QA inicial: health OK (51 elems DOOH restaurada), 5 presets, abas do BOM (Materiais/Plano de corte/Vento), exports pdf/dxf/gltf/bom.csv/cutlist.csv 200, marquee via gateway OK (422/404 iniciais eram payloads malformados do MEU teste curl — endpoint saudável). `POST /api/select/marquee 404` no dev.log do Next.js confirmado como ARTEFATO EXTERNO (requisição sem XTransformPort na origem :3000 — o helper api() do app sempre preserva o parâmetro). Traceback SyntaxError no server.log era transiente de edição da rodada anterior (ai_service.py:90 já corrigido; todos os módulos importam limpos).
- BUG PRÉ-EXISTENTE corrigido: list_projects() incluía data/_session.json como "projeto salvo" (o arquivo de sessão tem model+elements completos — aparecia na UI com o nome do projeto atual, ex. "Painel 4x2 m — 1 poste central"). Fix: pular arquivos que começam com "_" em list_projects. Agora a lista mostra só projetos reais.
- FEATURE histórico de versões (Python-first): services/project_service.py com _append_version() (arquiva {v, saved_at, name, preset_id, elements, mass_kg, cost_brl, model COMPLETO, bom_snapshot} em data/{pid}.versions.json; DEDUPE — se o snapshot BOM for idêntico ao da última versão, não cria versão nova; cap MAX_VERSIONS=10), read_versions() (metadados leves com fallback ao snapshot), load_version(), delete_version(); save_project() agora retorna version=N e o log do save diz "(vN arquivada)"; delete_project() remove também o .versions.json. Rotas: GET /api/projects/{pid}/versions · GET /api/projects/{pid}/versions/{v} (carrega na sessão, undo limpo) · DELETE …/versions/{v} · GET …/versions/{v}/diff (versão↔atual). UI: badge âmbar "🕘 Nv" no item do projeto → painel expansível (.ver-panel com animação) listando cada versão com data/elems/kg/R$ + [↥ carregar] [⇄ diff vs atual] [✕ excluir 2-passos]; toast do save mostra "versão vN arquivada"; badge some se 0 versões.
- FEATURE import de projeto JSON: rota POST /api/project/import aceita modelo puro, envelope {model:…} ou {data:…}; valida com ProjectModel (Pydantic) — rejeita com mensagem clara ("JSON sem 'elements'" / detalhe do validation error), exige elements não-vazio; adota na sessão com preset_id="" (card de preset desmarca), undo limpo, sessão persistida. UI: botão ⤒ no rodapé E ao lado do campo de salvar; <input type=file> oculto → FileReader → POST → adopt + toast "Projeto importado: NOME (N elems)". E2E: round-trip export→edit name→import OK (74 elems); lixo e schema inválido → toasts de erro amigáveis sem quebrar a sessão.
- FEATURE abrigo de vento (obstrução por painéis vizinhos): core/wind.py com OBSTRUCTION_LEVELS {0: sem, 0.2: parcial, 0.4: forte} + normalize_obstruction() (aceita "sem|parcial|forte", "20%", 20, 0.2; clamp 0..0.6) — reduz a força sobre o PAINEL por (1−obs); treliça exposta permanece integral (conservador). estimate_wind() retorna obstruction/obstruction_label e a nota cita o abrigo. Rota /api/wind?obs=. UI: controle segmentado "Abrigo de vizinhos: Nenhum/Parcial/Forte" na aba Vento (tooltips explicativos), bloco teal 🛡 quando ativo, hero/pressão anotados; RECALCULA o chip do card de resumo (renderProps) — atende sugestão da rodada anterior (direção/abrigo refletidos no chip: "🌬 Vento (V₀ 35 m/s · lateral · abrigo 20%)"). IA: "vento com obstrução forte", "vento lateral com abrigo 20%", "vento sem obstrução" → op __wind com obs; sendAi adota result.wind no chip.
- FEATURE diff A↔B entre salvos: project_service.py REFATORADO — diff_snapshots(old_snap, new_bom, meta_old, meta_new) é o motor único; diff_saved_vs_current / diff_two_saved / diff_version_vs_current usam o mesmo core (resposta agora com other_id/other_name/other_saved_at). Rota /api/projects/{pid}/diff?other=. UI: modal de diff ganhou <select> no header (#diffOther) com "↔ projeto atual" + outros salvos; trocar o select refaz o diff (openDiff(pid, other)); hero/cards renomeados A→B; rodapé explica o lado B com data de salvamento. E2E: QA8 Historico (parede, 284,5 kg) ↔ QA8 Outro (outdoor, 507,5 kg) → 9 itens (4 novos, 3 removidos, 2 alterados, +R$ 4.202,55) com TUBO Ø380/CHAPA DE BASE/CHUMBADORES entrando — bate com a troca de instalação.
- STYLE: .ver-badge (gradiente âmbar, hover lift, estado .open), .ver-panel (borda tracejada âmbar, animation verIn, max-height 200px scroll), .ver-row (vN monospace âmbar, meta truncada, icon-btns), .diff-cmp (select do comparador com focus ring teal), botão ⤒ no footer e no header de PROJETOS SALVOS, título do 💾 explica versionamento; ajuda ganhou 5 linhas novas (💾 versões, 🕘 painel, ⇄ A↔B, ⤒ importar, IA abrigo).
- Testes isolados em Python (28 checks, com DATA_DIR/SESSION_FILE redirecionados p/ tmp — lição da rodada 7): versionamento (v1→v2→dedupe, meta com kg/R$, load restaura perfil antigo, delete), diff A↔A=0, A↔B detecta troca de perfil, versão↔atual, salvo↔atual pós-refactor, import round-trip, obstrução (7,21 > 5,77 > 4,33 kN monotônico; treliça constante; F total consistente; normalize 5 variantes; nota cita abrigo), IA 3 comandos de abrigo. 27/28 no 1º run — o "FAIL" era índice [0] do MEU teste (ordenação alfabética), re-verificado por id: PASS.
- E2E final via agent-browser: save→badge "🕘 1v"→toast "v1 arquivada"; painel de versões com v1 (74 elems · 278 kg · R$ 5.523,95); IA mudou perfil V01→80x80x3 + save→"🕘 2v"; diff v2↔atual = 0 itens (recém-salvo, correto); diff A↔B via select = 9 itens; load v1 restaurou METALON_60x60x2 (74 elems); vento Forte → F 7,55→4,67 kN + chip "frontal · abrigo 40%"; IA "vento lateral com abrigo 20%" → toast completo + chip lateral/abrigo 20%; import JSON (upload de arquivo real) → 74 elems "QA8 Importado", preset desmarcado; erros de import amigáveis; delete v2 → badge 1v; mobile 390px sem scroll horizontal; reload restaurou sessão importada (persistência OK); lint EXIT 0; server.log sem erros novos.
- LIMPEZA: projetos de teste (qa8-historico c/ 2 versões, qa8-outro) e arquivos /tmp excluídos; sessão final restaurada: DOOH_4X2_1_POSTE, 51 elems, 507,53 kg, R$ 9.862,16 (conferido no reload final + screenshot).

Stage Summary:
- Serviço Python saudável na 3100; app estável; lint limpo; sessão limpa (DOOH padrão); nenhum projeto órfão.
- Novas capacidades: histórico de versões por projeto (v1/v2… com cap 10 e dedupe, carregar/diff/excluir pela UI), importação de projeto JSON com validação Pydantic (round-trip com o export), vento com abrigo por painéis vizinhos (3 níveis, IA incluída), comparador A↔B entre dois salvos (mesmo motor Python de diff).
- Arquivos alterados: services/project_service.py (versões + diff_snapshots + fix _session), core/wind.py (normalize_obstruction + OBSTRUCTION_LEVELS + estimate_wind obs), main.py (/api/project/import, 4 rotas de versões, diff?other, wind obs), services/ai_service.py (obs no vento + HELP_TEXT), static/index.html (badge/painel de versões, import com file input, select A↔B no diff, seg-ctl de abrigo, chip de vento com direção/abrigo, estilos .ver-*/.diff-cmp, ajuda).
- Próximos passos sugeridos: PDF do orçamento com página de diff (A↔B), DXF por vista em arquivos separados, restaurar versão sem sair da sessão (diff ↔ "restaurar" 1-clique), export do histórico de versões (zip), fator de obstrução por elemento (selecionar obstáculos no 3D), gráfico de evolução de custo/peso por versão (sparkline no painel de versões).

---
Task ID: 9 (cron review rodada 8)
Agent: Z.ai Code (webDevReview automático)
Task: QA completo via agent-browser + fase ESTÁVEL → 4 features novas (sparkline de evolução por versão, restaurar lado A com 1 clique no diff, DXF por vista em .zip, PDF do comparativo A↔B) + 1 bug pré-existente corrigido (import aceitava perfis inexistentes) + polimento de estilo (proj-item em 2 linhas) + 15 testes Python isolados.

Work Log:
- QA inicial: health OK (51 elems DOOH), todos os endpoints 200, exports 200, console sem erros. Últimos 5xx do server.log confirmados STALE (linhas 391/1446 de 2182 — bugs já corrigidos em rodadas anteriores).
- DESCOBERTA indireta de bug pré-existente: o uvicorn --reload (disparado pelas edições desta rodada) restaurou da sessão um fixture de teste antigo (painel 1x2 c/ rótulos "teste de importação") que estava em data/_session.json mascarado pelo processo velho — o que expôs também um 500 REAL no /api/export/gltf: KeyError "Perfil 'TUBO_200_t6.35' não existe no catálogo". CAUSA-RAÍZ: POST /api/project/import valida o schema com Pydantic mas NÃO validava os NOMES de perfil contra o catálogo — um JSON com perfil antigo/inexistente entrava, envenenava a sessão e derrubava GLTF/cutlist/wind com KeyError 500.
- BUG CORRIGIDO (main.py import_project): depois da validação Pydantic, cada el.profile é verificado com get_profile(); perfis inexistentes → HTTP 422 com mensagem clara ("Perfis não encontrados no catálogo: TUBO_200_t6.35. Corrija o JSON (os nomes válidos estão em /api/meta)..."). Testado via HTTP: JSON com TUBO_200_t6.35 → 422; JSON válido → 200; GLTF voltou a 200 (25 KB, 2 meshes) na sessão limpa. Sessão restaurada p/ DOOH padrão após os testes.
- FEATURE sparkline de evolução (drawing/sparkline.py, Python-first): SVG 300×86 gerado em Python puro (sem libs) com DUAS séries — peso (teal) e custo (laranja) — área com gradiente sob o custo, pontos com <title> nativo (tooltip vN — valor), destaque no último ponto, rótulos v1/vN, legenda compacta com último custo e escala de peso no eixo. Endpoint GET /api/projects/{pid}/versions/spark.svg (registrado ANTES da rota {v} p/ evitar 422 de parse int). UI: painel de versões (🕘) mostra o gráfico quando ≥2 versões, dentro de card .ver-spark com hover. Verificado E2E: img.naturalWidth > 0 com 2 versões reais.
- FEATURE restaurar 1-clique: modal de diff ganhou barra .diff-actions com botão teal "⤹ Restaurar “X” [vN] na sessão" + nota de aviso (sessão atual é perdida — salve antes). Contexto rastreado em lastDiff {kind: saved|version, pid, other?, v} setado por openDiff/openDiffVersion; restaurar versão → loadProjectVersion (existente), restaurar salvo → loadProject; fecha o modal + toast. Rótulo usa o nome base (remove sufixo "(vN)" que o backend anexa). E2E: diff v1↔atual → restaurar → V01 voltou de METALON_80x80x3 para METALON_40x40x2 (confirmado via /api/project) + modal fechou.
- FEATURE DXF por vista (.zip): drawing/dxf.py refatorado — _new_doc()/_draw_view() compartilhados + camada nova HACHURA (cor 8; antes hachura ia p/ CHAPAS) + view_to_dxf_bytes(view) que reposiciona a geometria p/ a origem (zoom extents limpo no AutoCAD) e grava metadados via ezdxf_metadata(). Rota GET /api/export/dxf.zip → ZIP com FRONTAL/TRASEIRA/LATERAL/SUPERIOR/INFERIOR.dxf + LEIAME.txt (projeto, data, nº de elementos, lista de camadas). Botão "⤓ DXF (.zip)" no modal de vista (ao lado do DXF único e do PDF). E2E: download 42 KB com os 6 arquivos.
- FEATURE PDF do comparativo (export/diff_pdf.py, reportlab): A4 paisagem com moldura, título + data, cards LADO A (âmbar) e LADO B (teal) com nome/data/elems/kg/R$, seta central, linha de Δ PESO e Δ CUSTO coloridos por sinal, tabela completa de itens com chips +/−/Δquant e valores antigos→novos + Δ CUSTO verde/vermelho, zebra, cap de 26 linhas c/ continuação, rodapé metodológico. ROTAS: GET /api/export/diff.pdf?pid=[&other=] (salvo↔atual ou salvo↔salvo) e GET /api/projects/{pid}/versions/{v}/diff.pdf (versão↔atual). Botão "⤓ PDF" no header do modal de diff (oculto quando d.ok=false). LIÇÃO: o canvas reportlab usa mm após c.scale(mm,mm) — fontes são EM MILÍMETROS (1ª versão veio 3× gigante e cortada; reescrita com métricas mm: título 5.4, células 2.6, tabela 259 mm). PDF verificado visualmente (Read no PDF): layout correto, totais batendo (169→159,2 kg / −R$ 191,91).
- STYLE: .proj-item reestruturado em 2 linhas (thumb + título/meta com ellipsis na 1ª linha, ações [🕘][⇄][↥][✕] alinhadas à direita na 2ª — antes os 4 botões comprimiam o texto em colunas de 1 palavra); .ver-panel max-height 250px + scrollbar âmbar; .ver-spark com hover; .diff-actions com flex-wrap (mobile-safe); ajuda ganhou 4 linhas novas (gráfico de evolução, restaurar lado A, PDF do comparativo, DXF .zip).
- Testes isolados em Python (15 checks, DATA_DIR/SESSION_FILE redirecionados p/ tmp — lição da rodada 7): versionamento v1/v2 + dedupe, metadados com kg/R$, sparkline (SVG válido, 2 séries, 4 tooltips, série única, vazio orientado), DXF por vista (5 vistas com SECTION/ENTITIES, camada HACHURA, vista vazia não explode), diff PDF (válido, sem itens, detecta troca de perfil, delta consistente), IA vento + plano de corte. 15/15 PASS.
- INCIDENTE durante o E2E (resolvido): minha 1ª edição do painel de versões deixou um parêntese desbalanceado no template literal → SyntaxError do module inteiro (boot travava no #loading sem erro visível no console da aba certa). Detectado extraindo o <script type="module"> e rodando node --check; corrigido; o check de sintaxe virou passo obrigatório após cada edição de JS (rodado mais 4× durante a rodada, sempre OK).
- LIMPEZA: projeto de teste qa9-feat (2 versões) excluído; sessão final restaurada e conferida no reload: DOOH_4X2_1_POSTE, 51 elems, 507,53 kg, R$ 9.862,16, F ≈ 7,88 kN; downloads de teste (comparativo PDF + zip) deixados em /home/z/Downloads como amostras; lint EXIT 0; server.log sem erros novos após o fix do import; mobile 390px sem scroll horizontal; JS module syntax OK.

Stage Summary:
- Serviço Python saudável na 3100; app estável; sessão limpa (DOOH padrão, nenhum projeto órfão); gltf/imports blindados.
- Novas capacidades: gráfico de evolução peso/custo por versão (SVG 100% Python), restauração de versão/projeto salvo com 1 clique a partir do comparativo, export DXF com um arquivo por vista (.zip + LEIAME + camada HACHURA própria), PDF imprimível do comparativo de versões (A4 paisagem).
- Correção importante: importação de JSON agora rejeita perfis fora do catálogo com mensagem acionável (antes envenenava a sessão e derrubava GLTF/cutlist/wind com KeyError 500).
- Arquivos novos: drawing/sparkline.py, export/diff_pdf.py.
- Arquivos alterados: main.py (rota spark.svg antes de {v}, diff.pdf ×2, dxf.zip, validação de perfis no import), drawing/dxf.py (refator p/ _new_doc/_draw_view + HACHURA + view_to_dxf_bytes com origem zero e metadados), static/index.html (ver-spark, diff-actions/restore/diffPdf, viewModalDxfZip, proj-item 2 linhas, lastDiff, ajuda), worklog.md.
- Próximos passos sugeridos: restaurar versão direto do painel 🕘 com confirmação estilizada (hoje o 1-clique está no diff), obstrução de vento por elemento (selecionar obstáculos no 3D), PDF do plano de corte (cards por barra comercial), import em lote (vários JSON), cache de thumbs com hash do conteúdo, export do histórico completo (.zip com todas as versões).

---
Task ID: 10 (cron review rodada 9)
Agent: Z.ai Code (webDevReview automático)
Task: QA completo via agent-browser + fase ESTÁVEL → 1 bug de infra corrigido (loop do iframe no acesso direto :3000) + 5 features novas (restaurar versão do painel 🕘 com confirmação estilizada, PDF do plano de corte, export do histórico .zip, tema claro/escuro com cena 3D acompanhando, métricas R$/m² e kg/m²) + 8 testes Python isolados.

Work Log:
- QA inicial: health OK (51 elems DOOH restaurada), 14 endpoints testados 200 (pdf/dxf/dxf.zip/gltf/bom.csv/cutlist.csv/json/views/wind/cutlist/bom/projects/history), BOM modal (3 abas), plano de corte 6 m com FFD, vento frontal V₀ 35 (F 7,88 kN), save + painel de versões, preset PAREDE carregado (74 elems · 277,55 kg · R$ 5.523,95). Fase ESTÁVEL → features. Únicos 500s do server.log confirmados STALE (linhas 2231/2337 de 2743 — KeyError TUBO_200_t6.35 da rodada 9, já corrigido; 0 erros após o último "Started server process").
- BUG DE INFRA corrigido (descoberto no próprio QA): abrir http://localhost:3000/ direto (sem o gateway :81) deixava a página PRETA em branco — o iframe src="/?XTransformPort=3100" resolvia para o PRÓPRIO Next.js (que não entende XTransformPort) e recarregava o wrapper recursivamente (iframe dentro de iframe, confirmado via contentDocument). FIX: next.config.ts com rewrites.beforeFiles — toda requisição com query XTransformPort=\d+ é proxyada para http://localhost:3100/:path*; beforeFiles é obrigatório (afterFiles rodava DEPOIS da rota de página e "/" continuava sendo servido pelo Next). Pelo gateway :81 o comportamento não muda (o Caddy intercepta antes de chegar ao Next). curl + E2E no browser confirmaram: :3000/?XTransformPort=3100 agora serve o app do FastAPI.
- FEATURE restaurar versão do painel 🕘 (sugestão da rodada 9): o ↥ do ver-panel agora é 2-passos (1º clique arma teal "restaurar?" por 3,2s — igual ao padrão de exclusão, mas com .icon-btn.arm teal em vez de vermelho; 2º clique restaura via loadProjectVersion). Tooltip novo explica "substitui o projeto atual; salve antes". Painel ganhou .ver-head ("Histórico (N de 10)" + botão ⤓ histórico (.zip)) e .ver-note explicando ↥/⇄/versionamento. E2E: largura 4000→5000 aplicada, save → v2 (87 elems · 334,0 kg vs v1 74 · 277,6 kg), restaurar v1 em 2 cliques → sessão voltou a 74 elems / 4000 mm (verificado via /api/project) e o histórico registrou "Versão v1 carregada".
- FEATURE PDF do plano de corte (export/cutlist_pdf.py, reportlab A4 paisagem em MM — lição da rodada 9 respeitada): cabeçalho com projeto/preset/data + chips "BARRA COMERCIAL N m" e "PERDA DE SERRA 3 mm"; strip de 4 stat boxes (barras a comprar com total de metros comerciais, peças a cortar, perfil bruto, sobra média/aproveitamento); UM CARD POR PERFIL com chips (pçs · metros · barras · sobra) e a RÉGUA DE CORTE de cada barra — retângulos proporcionais em teal alternado com separador branco (corte de serra) e sobra cinza, cap de 8 barras por card + nota "+N barras seguem o FFD", paginação automática com header de continuação; rodapé metodológico. Rota GET /api/export/cutlist.pdf?bar_len=; botão "⤓ PDF do plano" na aba (acima do CSV). 3 correções no 1º render (achado no Read do PDF): sub do stat com unidade errada ("0.1 m" — bar_m já é metros), nota "+1 barras" sobrepondo a última régua (card_h não contava a nota) e plural "1 barras" → tudo corrigido e re-verificado em 6 m e 12 m (12 m fecha em 1 página: 16→8 barras).
- FEATURE export do histórico .zip (services/project_service.build_versions_zip): LEIAME.txt (projeto, data do export, nº de versões, instruções de reimportação via ⤒/POST /api/project/import) + versoes/vN.json (entrada completa: model + bom_snapshot — cada uma reimportável) + evolucao.svg (sparkline, quando ≥2 versões). Rota GET /api/projects/{pid}/versions/export.zip registrada ANTES da rota {v} (mesma lição do spark.svg — senão 422 no parse int). Botão no ver-head de qualquer projeto. Testado via curl (zip válido, LEIAME citando o projeto, v1.json com model+snapshot) e via UI (toast "Download iniciado: historico_qa10-rodada.zip").
- FEATURE tema claro/escuro (🌙/☀️ na toolbar, ao lado do ⤢): CSS refatorado — novas vars --card/--hover/--line-soft/--viz-rest/--selbg/--selink/--danger-soft e ~30 hardcodes (#fff, #f0fdfa, #fef3c7, #eef1f5, rgba(255,255,255,…)) trocados por vars; bloco html[data-theme="dark"] redefine tudo (slate #0d1524/#141f33, texto #e6edf7, teal mantido) com overrides diretos para gradientes (.ps-cost teal-escuro, .ps-wind âmbar-escuro, .wind-effect, .warn-strip, .ver-panel, .diff-actions, .batch-head, .tree-item.msel, .tree-all, .batch-chip, .viewport). Cena three.js acompanha: THEMES com bg/ground/cores das 2 grades; applyTheme() recria GridHelpers (const→let) com dispose, resolve visibilidade com o chip Grade, persiste em localStorage("ledcad_theme") e aplica no boot. E2E: toggle nos 2 sentidos, recarregou a página e o tema escuro PERSISTIU; chips da toolbar legíveis (antes ficavam brancos — .chip usava rgba(255,255,255,.92), corrigido). Bônus de acessibilidade: :focus-visible global teal + transição suave de cor em painéis/botões/inputs.
- FEATURE R$/m² e kg/m²: card de resumo ganhou 2 células (Custo/m² e Peso/m² = totais ÷ largura×altura do painel) com tooltip explicando o uso (comparar opções, precificar aluguel). Conferido em 2 projetos: DOOH 8 m² → R$ 1.232,77/m² · 63,4 kg/m²; PAREDE 8 m² → R$ 690,49/m² · 34,7 kg/m² (batem com os totais).
- IA inalterada (comandos continuam funcionando — "vento lateral" testado pela barra: respondeu com F ≈ 1,2 kN e detalhes das fixações do projeto de parede).
- Testes isolados em Python (8 checks, DATA_DIR redirecionado p/ tmp — lição da rodada 7): cutlist PDF (6 m, 12 m e vazio — todos %PDF), zip (v1 arquivada, LEIAME citando o projeto, v1.json com model+snapshot, troca de perfil → v2, BOM idêntico → DEDUPE não cria v3, evolucao.svg válido com 2 versões, pid inexistente → None, delete limpa). 8/8 PASS. Dois "FAILs" intermediários eram bugs do MEU teste (mutava o elemento[0] = painel; start/end são Vec3 com .x/.y/.z, não lista; e mover barra não muda o BOM — dedupe correto).
- LIMPEZA: projetos de teste excluídos (qa10-rodada com 2 versões e painel-4x2-m-fixado-em-parede-12-09-2026 criado sem querer ao salvar com o campo nome vazio — o nome vem do model.project.name + data); sessão final restaurada: DOOH_4X2_1_POSTE, 51 elems, 507,53 kg, R$ 9.862,16, R$ 1.232,77/m² (conferido no reload final). Lint EXIT 0; JS module syntax OK (node --check após cada edição do index.html); mobile 390px sem scroll horizontal (tema claro e escuro).

Stage Summary:
- Serviço Python saudável na 3100; app estável; sessão limpa (DOOH padrão); nenhum projeto órfão; lint limpo; 0 erros novos no server.log.
- Correção importante de infra: acesso direto à porta 3000 agora funciona (proxy via rewrites do Next.js) — antes o iframe do wrapper carregava a si mesmo e a tela ficava branca. O caminho do usuário (gateway :81) não mudou.
- Novas capacidades: restauração de versão com confirmação estilizada direto no painel 🕘, PDF do plano de corte (folha de serralheria A4 paisagem com régua de corte por barra), export do histórico completo em .zip reimportável, tema claro/escuro persistido com a cena 3D acompanhando, métricas de custo e peso por m² no resumo.
- Arquivos novos: export/cutlist_pdf.py.
- Arquivos alterados: next.config.ts (rewrites beforeFiles), services/project_service.py (build_versions_zip), main.py (rotas cutlist.pdf e versions/export.zip), static/index.html (tema escuro com ~30 vars novas/overrides, chipTheme + applySceneTheme, ver-head/ver-zip/ver-note/arm de restauração, cutPdf, células R$/m² e kg/m², ajuda com 4 linhas novas), worklog.md.
- Próximos passos sugeridos: obstrução de vento por elemento (selecionar obstáculos no 3D), import em lote de vários JSON, PDF do orçamento com página de diff incluída, cache de thumbs por hash do conteúdo, escolha de tema acompanhando prefers-color-scheme no 1º acesso, export .zip com projeto + todas as vistas DXF (pacote completo de entrega).

---
Task ID: 2
Agent: Z.ai Code (principal)
Task: Remover tema azul/escuro do LED STRUCTURE CAD — interface 100% clara com fundo branco (pedido do usuário: "essa cor azul nao me agrada deixe ele com fundo branco mesmo todo claro esse escuro nao ta ajudando na visibilidade").

Work Log:
- Removido por completo o tema escuro (navy #0d1524/#141f33/#1a2740) do static/index.html: bloco CSS html[data-theme="dark"] inteiro, regras .ver-zip dark, botão 🌙 chipTheme da toolbar, listener de clique, lógica localStorage ledcad_theme e detecção prefers-color-scheme — o app agora tem TEMA ÚNICO CLARO.
- Fundo branco em tudo: --bg #eef1f5→#ffffff, viewport 3D com gradiente branco puro, cena Three.js scene.background 0xffffff, solo 0xf2f2f4, grades neutras (0xd6d6da/0xe6e6e9/0xeeeeef/0xf5f5f6), sombras e overlays neutralizados (modal, toast, vp-measure).
- Azul eliminado da interface: arrow do eixo Y 0x3b82f6→verde 0x16a34a, snap "center" 0x0ea5e9→teal 0x14b8a6, tag k-chapa indigo (#e0e7ff/#4338ca)→stone, hover emissive 0x3f4b5c→0x4a4a51, famílias slate (ink/muted/line/bom header)→cinza neutro (zinc).
- Barras 3D em cinza-aço neutro (post 0x3a3a41, vertical 0x4c4c53, horizontal 0x5e5e65, trave 0x8a8a91, diagonal 0x9a9aa1, chapa 0x5a5a62) + legenda do painel esquerdo atualizada em par.
- drawing/projections.py: LAYERS hachura #94a3b8→#a1a1aa e eixo #cbd5e1→#d4d4d8 (vistas 2D 100% neutras sobre branco); serviço recarregou sozinho (--reload).
- Verificado com agent-browser: / (rota do usuário, iframe :3100) renderiza claro, sem erros de console, APIs 200; interação testada (aba LATERAL troca câmera 3D + desenho 2D); screenshot final confirma legibilidade.

Stage Summary:
- Tema único claro permanente com fundo branco (nenhum modo escuro restante; localStorage antigo é ignorado).
- Contraste melhorado: estrutura em cinza-aço sobre branco, desenhos 2D em traço escuro sobre branco.
- Arquivos: mini-services/led-cad/static/index.html, mini-services/led-cad/drawing/projections.py.

---
Task ID: 3
Agent: Z.ai Code (principal)
Task: Usuário ainda via o tema ESCURO (screenshot): só o plano 2D (export) estava claro. Causa raiz: CACHE do navegador — o index.html antigo (com dark) ficou em cache porque o StaticFiles não enviava Cache-Control.

Work Log:
- Diagnóstico: curl confirmou que o servidor já entrega o HTML CLARO (direto :3100 e via rewrite do Next :3000 → grep "TEMA ÚNICO" = 1 nos dois), mas a resposta NÃO TINHA Cache-Control (só etag/last-modified) → heurística do navegador serviu cópia velha do tempo do tema escuro (localStorage ledcad_theme="dark" do usuário reforçava o dark na cópia antiga).
- FIX 1 (mini-services/led-cad/main.py): subclass NoCacheHTMLStaticFiles(StaticFiles) que injeta "Cache-Control: no-cache" em toda resposta de arquivo estático — navegador sempre revalida via etag; montagem "/" trocada para a nova classe. Uvicorn --reload aplicou na hora (header confirmado via curl -I).
- FIX 2 (src/app/page.tsx): iframe src ganhou cache-buster "&v=2" → URL nova que nenhum cache tem, garantindo o fetch do HTML claro imediatamente, mesmo em navegadores com a cópia antiga.
- E2E agent-browser via / (:3000, iframe v=2 → rewrite → FastAPI): página 100% clara (painéis brancos, viewport 3D branco, estrutura cinza-aço), console SEM erros, sessão do usuário preservada (39 elementos restaurados — o projeto importado que aparecia no screenshot dele).

Stage Summary:
- Causa do "ainda está azul" era cache do navegador, não código — o tema escuro já estava 100% removido no Task 2.
- Agora blindado: Cache-Control: no-cache no estático + cache-buster v=2 no iframe. Futuras atualizações do HTML nunca mais presas em cache stale.
- Arquivos alterados: mini-services/led-cad/main.py (NoCacheHTMLStaticFiles), src/app/page.tsx (iframe v=2).

---
Task ID: 4-a
Agent: general-purpose (ai-gateway)
Task: Gateway Node ai-gateway (:3101) wrapper do z-ai-web-dev-sdk p/ conector real de IA do led-cad

Work Log:
- Criado mini-service isolado em mini-services/ai-gateway (Bun 1.3.14 + TypeScript, ZERO novas dependências — o SDK z-ai-web-dev-sdk 0.0.18 já resolvido do node_modules raiz via resolução upward; config /etc/.z-ai-config confirmada presente).
- package.json com scripts dev (bun --hot index.ts) e start (bun index.ts); porta FIXA 3101 hardcoded (sem env var).
- index.ts: Bun.serve com GET /health ({ok, service:"ai-gateway", model:"glm", uptime_s}), POST /chat (validação: messages array não-vazio, cada item role+content strings → 400 descritivo caso contrário; role "system" mapeado para "assistant" antes da chamada — requisito do SDK; thinking:{type:'disabled'} fixo; temperature do request intencionalmente IGNORADO por segurança — não é repassado ao SDK), resposta {ok:true, content, finish_reason}, erro de SDK → 502 {ok:false, error}, path desconhecido → 404, JSON inválido → 400; Content-Type: application/json + CORS permissivo em todas as respostas (inofensivo server-to-server); cliente ZAI único reutilizado (module-level let zaiClient) e create() falho NÃO é cacheado (retry na próxima request); try/catch global → 500 JSON.
- Start via nohup &/setsid morria ao fim do comando do sandbox (processo reapado); SOLUÇÃO: start-stop-daemon --start --background --make-pidfile (double-fork, PPid=1, mesmo padrão dos services pré-existentes) → PID 32615 estável (bun run dev, listener bun pid 32617), sobrevive entre comandos, uptime crescente, server.log SEM crash loops (2 linhas de boot limpas: a 1ª do setsid efêmero, a 2ª do daemon atual).
- Testes: /health 200 ok; /chat real GLM "Responda apenas: OK" → {"ok":true,"content":"OK","finish_reason":"stop"} HTTP 200 (~2s); /chat com system+user+temperature → "Sim." (mapeamento system→assistant OK, temperature ignorado OK); 4 casos de validação → 400 com mensagens precisas (array vazio, role inválida, content não-string, JSON malformado); /other → 404 {"ok":false,"error":"not found"}. Nenhum outro arquivo do projeto foi tocado (nem Python, nem index.html).

Stage Summary:
- ai-gateway NO AR e estável em http://localhost:3101 (PID 32615, pidfile gateway.pid, log server.log) — wrapper HTTP do z-ai-web-dev-sdk (GLM gratuito, thinking disabled) pronto para o conector real de IA do led-cad.
- Endpoints: GET /health → {"ok":true,"service":"ai-gateway","model":"glm","uptime_s":N}; POST /chat → body {"messages":[{role:"user"|"assistant"|"system", content},...], temperature ignorado} → 200 {"ok":true,"content":str,"finish_reason":str|null}; erros → 400 validação / 502 SDK / 404 rota.
- Evidência de teste: POST /chat {"messages":[{"role":"user","content":"Responda apenas: OK"}]} → HTTP 200 {"ok":true,"content":"OK","finish_reason":"stop"} (chamada REAL ao GLM); health uptime_s monotônico entre chamadas (3 → 9 → 43.7) confirmando daemon vivo.
- Contrato pensado p/ o Python (3100) chamar server-to-server: basurl http://localhost:3101, CORS aberto (inofensivo), erro de IA sempre {ok:false, error} — fácil de tratar no ai_service.py.

---
Task ID: 4 (4-a subagente + 4-b/4-c principal)
Agent: Z.ai Code (principal) + general-purpose (4-a)
Task: Conector REAL de IA (z.ai grátis/Gemini/OpenAI/compatível), prompt com o modelo JSON da ferramenta, assistente de novo projeto (formulário + IA), novo em branco e ferramentas livres (Barra A→B com perfil escolhido) — pedido: "o sistema não tem conexão real com IA, só comandos pré-estabelecidos; precisa de JSON modelo para a IA criar peças do zero, formulário de novo com perguntas, e criação livre do zero".

Work Log:
- 4-a (subagente): mini-services/ai-gateway (Bun, porta 3101) wrapper do z-ai-web-dev-sdk — POST /chat (mapeia role system→assistant, thinking disabled), GET /health; testado com chamada GLM REAL ("OK"/"PONG"). Rodando via start-stop-daemon (PID 32615).
- 4-b Python: services/ai_llm.py (registro de provedores zai[grátis, via gateway :3101]/gemini[OpenAI-compat, tier grátis]/openai/compat[base_url custom — Groq/DeepSeek/Ollama…], config persistida em data/ai_config.json com chaves SEMPRE mascaradas na API, chat_completion + test_provider via urllib stdlib); services/ai_prompts.py (PROMPT DE SISTEMA que ensina a IA a USAR A FERRAMENTA: coordenadas mm X/Y-Z, modelo JSON canônico, catálogo de 16 perfis com kg/m, catálogo completo de operações, regras [chapas ≤800mm — a IA tinha criado chapa 6x3m de 3.532 kg], contrato {explain, ops}, exemplos few-shot, estado atual do projeto+BOM+elementos); ai_service.py refatorado (execute_ops extraído p/ servir parser rápido E LLM; op __blank); ProjectStore.new_blank() em core/operations.py; main.py: /api/ai/command reescrito (auto=LLM real com fallback offline p/ parser rápido | llm | quick; extrai JSON tolerante a fences), /api/ai/config GET/POST, /api/ai/test (conexão real), /api/project/blank.
- 4-c Frontend: modal ⚙ CONECTOR IA (provedor/chave mascarada/modelo/base URL/hint por provedor, 🔌 testar conexão com resultado real, salvar); modal ✨ NOVO PROJETO assistente (tipo outdoor/parede/suspenso/rental/em branco, L×A, profundidade, pé-direito, postes, perfil, nome, descrição livre; "Criar com IA" envia form+texto ao LLM real, "Criar direto" aplica preset/regen sem IA); botão ⬚ EM BRANCO nos PRESETS (2-passos arm) que já entra no modo Barra; SELETOR DE PERFIL na toolbar da Barra A→B (16 perfis com kg/m, populado de /api/meta — antes era hardcoded METALON_40x40x2); barra IA com placeholder de pedidos livres + toast "✨" quando engine=llm; ajuda com 4 linhas novas.
- TESTES: /api/ai/test → PONG real 0.32s; IA criou PÓRTICO DO ZERO via modo llm (__blank + 2 postes METALON_100x100x3 a ±2000 + travessa no topo — coordenadas exatas, BOM 89,6 kg, explain em pt-BR); wizard UI → painel suspenso com TIRANTES (11 elems) a partir do formulário+descrição; auto mode → engine llm (travessa+diagonal X adicionadas); fallback: provider quebrado em auto → engine quick respondeu custo; chave fake em modo llm → mensagem clara (CORRIGIDO 500: except usava json.JSONDecodeError sem import — JSONDecodeError é subclasse de ValueError); ⬚ em branco → 0 elems + preset BLANK; Barra A→B livre com perfil METALON_100x100x3 escolhido no seletor (BX01 criada por cliques sintéticos); config mascarada (AIzaFA…cdef); node --check OK; lint OK; sessão restaurada (DOOH 51 elems).
- NOTA: 500s transitórios em /api/views durante janela de reload do uvicorn (auto-resolve; request atual 200/23ms).

Stage Summary:
- IA 100% REAL conectada por API: z.ai GLM gratuito (padrão, sem chave), Gemini, OpenAI e qualquer compatível OpenAI — conector configurável em ⚙ com teste de conexão; parser rápido permanece como fallback offline.
- A IA entende A FERRAMENTA (modelo JSON + operações + catálogo) e cria peças do zero; usuário também pode montar do zero: ⬚ em branco + Barra A→B com perfil escolhido.
- Arquivos novos: mini-services/ai-gateway/{index.ts,package.json}, services/ai_llm.py, services/ai_prompts.py. Alterados: services/ai_service.py, core/operations.py, main.py, static/index.html, worklog.md.

---
Task ID: 4-r (recuperação pós-restart do sandbox + fixes de segurança da IA)
Agent: Z.ai Code (principal)
Task: (1) Build Error do Next.js (Module not found @vercel/turbopack-next/internal/font/google/font — timeout baixando Geist de fonts.gstatic.com); (2) Serviços :3100/:3101 mortos após restart do sandbox; (3) BUG GRAVE descoberto: no modo auto o LLM respondia "desfaça" com __blank, APAGANDO o projeto do usuário (51 elems → 1).

Work Log:
- FIX FONTE: removido next/font/google (Geist/Geist_Mono) de src/app/layout.tsx — era timeout de rede para fonts.gstatic.com no sandbox (Turbopack). globals.css agora usa font stack do sistema (ui-sans-serif/system-ui…). GET / 200 no dev.log; zero erros de fonte.
- SERVIÇOS: descoberta chave — start-stop-daemon faz chdir para "/" POR PADRÃO (bun/python não achavam package.json/main.py). Solução: flag -d <dir> + wrapper _start.sh com log em server.log. :3101 (ai-gateway, bun --hot) e :3100 (uvicorn --reload) recriados como daemons estáveis.
- LIMPEZA: chave Gemini FAKE de teste (AIzaFAKE-…) removida de data/ai_config.json (has_key agora tudo false; z.ai segue padrão sem chave).
- RECUPERAÇÃO DE SESSÃO: o reset __blank tinha limpo a sessão do usuário; restaurada via undo em cadeia do parser rápido (mode:"quick") até voltar aos 51 elementos; elemento de teste BX01 (travessa do meu teste de IA) excluído → sessão EXATA do usuário de volta (507,53 kg · R$ 9.862,16 · DOOH_4X2_1_POSTE).
- FIX SEGURANÇA 1 (main.py /api/ai/command): modo auto agora roda o PARSER RÁPIDO PRIMEIRO — comandos determinísticos (desfaça, custo, preset, exclua ID…) nunca mais chegam ao LLM (nada de __blank indevido) e respondem sem latência. Parse falhou → IA real; parse OK mas execução falhou (ex. "Nada para desfazer.") → responde o erro real na hora com ok:false/engine:quick (a IA só inventaria desculpa).
- FIX SEGURANÇA 2 (services/ai_prompts.py): regra 3 reescrita — __blank APAGA TUDO, só usar com pedido EXPLICITAMENTE novo/em branco/limpar; nova regra 7 — NUNCA __blank/__preset/__regen para pedidos de desfazer/corrigir (ops [] + explain).
- E2E agent-browser: rota / (:3000) clara, console limpo, sessão 51 elems restaurada visível no painel (PAINEL 4.000×2.000×800 · PD 3.000 · 507,53 kg · R$ 9.862,12); modal ⚙ CONECTOR IA → "✅ Conectado: z.ai GLM — gratuito (padrão) · modelo glm · respondeu PONG em 0.23s"; modal ✨ NOVO PROJETO (form com perguntas + "Criar com IA" e "Criar direto (sem IA)") verificado — FECHADO sem criar (sessão preservada); roteamento testado: "desfaça"→quick instantâneo honesto; "quanto custa"→quick (__cost, R$ 9.862,16); pergunta livre sem keyword→engine llm com ops [] (piada do engenheiro 😄, sem mutação); dev.log sem erros.

Stage Summary:
- Build Error do usuário RESOLVIDO (fonte externa removida; :3000 estável em 200).
- Serviços recuperados com padrão de daemon correto (-d): led-cad :3100 + ai-gateway :3101 (GLM gratuito real).
- IA REAL conectada e SEGURA: auto = parser rápido p/ comandos conhecidos + LLM (z.ai/Gemini/OpenAI/compatível) p/ linguagem livre; prompt ensina a ferramenta (modelo JSON + operações + catálogo) com guarda-rails anti-destruição.
- Sessão do usuário preservada (51 elems) — incidente __blank resolvido e impossibilitado no futuro (dupla camada: roteamento quick-first + regras no prompt).
- Arquivos alterados: src/app/layout.tsx, src/app/globals.css, mini-services/led-cad/main.py, mini-services/led-cad/services/ai_prompts.py, mini-services/led-cad/data/ai_config.json; novos wrappers: mini-services/{ai-gateway,led-cad}/_start.sh.

---
Task ID: 4-s (fix "IA não obedece ao criar" — sequestro de verbos de criação + guias)
Agent: Z.ai Code (principal)
Task: Usuário: "estou testando com a ia do z.ai que está lá configurada, até recebo resposta de como está, mas não obedece ao criar — coloque um prompt que ela aceita ou um formulário de como devo preencher".

Work Log:
- DIAGNÓSTICO (3 bugs encadeados): (1) o parser rápido capturava verbos de criação — "crie um painel 6x3 com 2 postes" casava com a regra de dimensão 'painel LxA' → disparava __regen em vez de ir para a IA; (2) __regen com preset "BLANK" → "Preset 'BLANK' não encontrado." (preset_id ficou BLANK após a cadeia de undos da recuperação — o undo não restaurava preset_id); (3) sem rede de segurança quando o LLM respondia criação com ops [].
- FIX 1 (services/ai_service.py parse_command): guarda ANTES das regras de resize — verbos de criação (crie/criar/cria/faça/monte/construa/gere/desenhe/projete/do zero/pórtico) levantam OperationError → caem para a IA real (que monta do zero com add_* e/ou __blank). Comandos elementares (adicionar poste/travessa, mover, excluir, resize puro "largura 5000") continuam no parser rápido — regressão testada: 6/6 rotas corretas.
- FIX 2 (core/operations.py): _snapshot/undo agora incluem/restauram preset_id — desfazer um __blank não deixa mais "BLANK" zumbi (causa raiz do erro de preset visto pelo usuário).
- FIX 3 (services/ai_service.py __regen): preset BLANK/inexistente → mensagem clara orientando ("Este projeto está EM BRANCO… peça à IA: 'crie…' ou use ✨/⬚") em vez de exceção crua.
- FIX 4 (main.py llm path): pedido com verbo de criação + ops [] → UMA repetição automática exigindo ops ("Seu JSON veio com ops:[] mas o pedido é de CRIAÇÃO…") — rede de segurança para preguiça do LLM.
- UI (static/index.html): (a) CHIPS "PEDIDOS PRONTOS — 1 CLIQUE" acima da barra IA (6 receitas: Pórtico do zero, Painel 6x3 suspenso, Diagonal em X, + Poste, Quanto custa?, Vento lateral — clique preenche e envia; CSS .ai-chip pill + hover teal, .ai-box virou coluna com .ai-row); (b) AJUDA (?) com duas seções novas: "📜 RECEITAS PRONTAS (copie e cole)" (7 exemplos com o que cada um faz, incluindo "crie um projeto novo em branco" marcado como APAGA tudo) e "✨ Como preencher o NOVO PROJETO (formulário)" (campo a campo, destacando "Descreva como construir" como o campo mais importante) + dica final (verbos de criação geram ops; perguntas só respondem; Ctrl+Z desfaz).
- E2E: node --check OK; "crie um painel 6x3 com 2 postes" via API → engine llm, __blank+2 postes+travessas = 12 ELEMENTOS CRIADOS DO ZERO (antes: "Preset 'BLANK' não encontrado."); chip "Quanto custa?" clicado no navegador → respondeu; chip "+ Poste" clicado → "Poste POSTE-02 criado em X=1200" no histórico + 2º poste visível no 3D + métricas 582,77 kg · R$ 11.333; sessão do usuário restaurada depois (51 elems, POSTE-02 desfeito, preset DOOH_4X2_1_POSTE — o fix do undo mantém o preset correto); rota / (:3000) serve o HTML novo (no-cache), console sem erros.
- Mutação de teste removida; dados/_session.json deixa a sessão exata do usuário (507,53 kg · R$ 9.862,16).

Stage Summary:
- "IA não obedece ao criar" RESOLVIDO na causa raiz: pedidos de criação chegam 100% à IA real (GLM), que devolve ops validadas executadas pelo motor Python; resposta da frase exata do usuário agora cria a estrutura completa.
- Usuário tem 3 caminhos guiados: chips de 1 clique na barra IA, receitas copiáveis na ajuda (?), e formulário ✨ documentado campo a campo.
- Arquivos alterados: mini-services/led-cad/services/ai_service.py, core/operations.py, main.py, static/index.html.
- Próximos passos sugeridos: streaming da resposta da IA na barra (feedback enquanto pensa), histórico de conversa da IA (últimas N mensagens como contexto), botão "repetir" no último pedido.

---
Task ID: 5 (direção nova: CAD de desenho LIVRE + prompt pronto "painel 2x1 gabinetes")
Agent: Z.ai Code (principal)
Task: Usuário (2 mensagens): (1) "painel 2x1 com gabinetes de 096x0,96 cada para painel outdoor… faça o prompt aqui pra eu enviar… precisamos ter isso padrão de texto"; (2) MUDANÇA DE DIREÇÃO — "a ideia não é a IA usar os presets, mas criar do zero sempre tudo que eu quiser usando a ferramenta… CAD de estrutura metálica que a IA entenda como editar o JSON e criar livremente QUALQUER coisa (círculos, cubos, casas)… não quero comandos manuais que não envolvam a IA… o foco não é preço, é o esboço estrutural com medidas e materiais".

Work Log:
- DIAGNÓSTICO do "não obedece ao criar" com a frase exata do usuário (backup de data/_session.json ANTES): "painel 2x1 com gabinetes…" (sem verbo) era capturado pela regra 'painel LxA' do parser rápido → __regen → erro (preset BLANK). 
- NOVA ARQUITETURA (direção do usuário): (1) parser rápido agora tem SÓ UTILIDADES (desfaça/histórico/custo/vento/cutlist/preset/salve preset/seleção) — TODO o resto (criação, edição, perguntas abertas com qual/como/devo…) vai 100% para a IA real; (2) prompt do sistema REESCRITO: "COPILOTO DE DESENHO" de CAD de esboço livre — SEM __preset/__regen no catálogo da IA, SEM preço, com padrões de desenho (passarela, torre, casa, gaiola de gabinetes, painel circular) e regra __blank para estrutura NOVA COMPLETA (desfazível) / add_* para acréscimo.
- MOTOR — 4 operações novas de desenho livre (core/operations.py, 1 op = 1 undo): set_panel (dimensiona/oculta a referência do painel, com ground_clearance), add_circle (anel/polígono 3–48 segmentos, planos frontal/horizontal/lateral, radius exato verificado), add_box (12 arestas de paralelepípedo, center+dims OU corner1/corner2), add_grid (grelha cols×rows no módulo de gabinetes, border opcional, plano inferido). Testes unitários: raio ok, 12 arestas, grelha 3V+2H, cadeia de undo ok.
- BUG CRÍTICO achado via logs: /api/views → 500 (IndexError em dimensions.py:125) com estrutura livre da IA (sem grupo QUADRO/role trave). FIX: _vertical_zs/_vertical_xs caem para o envelope de TODAS as barras + guardas len>=2 em SUPERIOR/LATERAL. Vistas 2D agora dimensionam qualquer esboço.
- BUG CRÍTICO 2 achado via log [ai]: GLM truncava o JSON (max_tokens default ~1k) → "Expecting ':' delimiter char 998" no pedido do círculo. FIX duplo: gateway passa max_tokens:8192 (SDK repassa ...body) + _repair_truncated_json no main.py (fecha chaves/colchetes abertos, descarta fragmento final). Logging [ai] adicionado ao fallback.
- main.py: retry de criação com ops:[] ampliado (verbos + substantivos estruturais: gabinet/passarela/círculo/cubo/casa/torre/pórtico/estrutura/painel, e não-pergunta).
- UI (static/index.html): chips viraram DESENHO LIVRE — 🧱 Painel 2×1 (0,96) + poste + passarela (O PROMPT PRONTO), ⭕ Painel circular Ø2 m, ◼ Cubo 1 m, 🏠 Casa 4×3 m, 🪄 Pórtico 4 m, ⤹ Desfaça; placeholder da barra IA reescrito; R$ REMOVIDO do status (só mm + kg); ajuda (?) com seção "🤖 IA — DESENHO LIVRE", "🧱 PADRÃO DE PEDIDO — copie, preencha e envie" (o template) e receitas novas; HELP_TEXT (parser) atualizado. FIX próprio: shadow de variável `st` que apontaria para #statusModel inexistente.
- E2E API: frase EXATA do usuário → engine llm, 16 ops (set_panel 1920×960 + add_box gaiola + add_grid frente/trás + add_post + add_grid piso passarela + 3 add_plate + 5 add_beam guarda-corpo) ✓; cubo → __blank+set_panel visible:false+add_box (12 elems) ✓; círculo → add_circle 16 seg + 2 tirantes (18 elems) ✓ (após fix max_tokens).
- E2E browser: chip 🧱 clicado → IA desenhou tudo (screenshot: painel 1.920×960, 600 mm, PD 3.000, 397,62 kg, grupos GAIOLA/GAIOLA FRENTE/GAIOLA TRAS/POSTES/PASSARELA PISO no histórico, VISTA FRONTAL 2D com cotas); modal ajuda com o padrão de pedido visível; console limpo; sessão do usuário RESTAURADA (51 elems, 507,53 kg — backup data/_session.backup-51elems.json mantido).
- Cron webDevReview recriado (job 383114, fixed_rate 900s) com o contexto da nova direção.

Stage Summary:
- O sistema agora é o que o usuário pediu: CAD de esboço estrutural LIVRE — a IA (z.ai GLM real) desenha QUALQUER coisa do zero (painéis por gabinetes, círculos, cubos, casas, torres, passarelas) sem presets e sem preço; parser rápido só utilidades; 3 caminhos de uso: desenho manual (Barra A→B), painéis salvos (presets), ou IA.
- PROMPT PRONTO do caso dele (funciona, testado): "Crie um painel outdoor NOVO: 2 colunas × 1 fileira de gabinetes de 0,96×0,96 m (painel 1920×960 mm), 1 poste central, pé-direito 3 m, gaiola no módulo dos gabinetes e passarela de manutenção atrás (650 mm) com guarda-corpo de 1,10 m" — também é chip de 1 clique e está na ajuda como PADRÃO DE PEDIDO preenchível.
- Arquivos: core/operations.py (+4 ops), services/ai_service.py (parser utilitário + HELP_TEXT + msgs), services/ai_prompts.py (reescrito), main.py (JSON repair + retry ampliado + log), drawing/dimensions.py (fallback envelope), static/index.html (chips/ajuda/status), ai-gateway/index.ts (max_tokens 8192).
- Riscos/próximos: GLM gratuito tem falhas transitórias (fallback honesto); streaming/histórico de conversa como próximo passo; testar casa/torre E2E; (sessão do usuário protegida em _session.backup-51elems.json).

---
Task ID: 6
Agent: Z.ai Code (principal)
Task: REFATORAÇÃO DE UX/UI (pedido explícito) — "Paint + SketchUp simplificado": NÃO alterar o motor geométrico/backend/schema/presets/PDF; interface escura, limpa e técnica com UM ÚNICO viewport, painéis todos sob demanda.

Work Log:
- REESCRITO APENAS static/index.html (motor Python, JSON, API, presets, exports, operações 100% intocados — nenhum arquivo .py alterado). Sessão do usuário preservada: resíduo de teste (70 elems BLANK) restaurado do backup canônico → 51 elems DOOH_4X2_1_POSTE (507,53 kg) + restart do serviço.
- HEADER MÍNIMO: LED STRUCTURE CAD + [＋Novo] [⤒Importar JSON] [✨IA] [💾Salvar] [🖨Gerar Projeto] (+ ☰Elementos e ? à direita). Removidos da tela principal: painel esquerdo (presets/projetos/painel mm/árvore), faixa de vistas 2D inferior, painel de propriedades fixo, histórico fixo, barra IA permanente, JSON/BOM permanentes.
- VIEWPORT ÚNICO (~90% da área útil): viewbar flutuante topo-centro com 3D/Frente/Fundo/Esquerda/Direita/Superior/Inferior — a MESMA câmera reposiciona (nunca 2 vistas simultâneas); INFERIOR novo (maxPolarAngle 0.995π). Stats discretos (mm · PD · elems · kg, SEM preço) + hint de atalhos no rodapé do viewport.
- TOOLBAR VERTICAL esquerda com exatamente 9 ferramentas: Selecionar/Mover(gizmo)/Barra(popover c/ 16 perfis kg/m)/Poste(clique no solo → add_post c/ pé-direito e perfil do projeto)/Chapa(2 cliques A→B → add_plate 10 mm)/Duplicar/Excluir/Desfazer/Refazer. Régua (R) e Área (M) continuam como atalhos ocultos.
- REFIZER (novo, client-side): opsStack/redoStack — aplica op → push; undo → move p/ redo; Ctrl+Shift+Z/Ctrl+Y ou botão ↻ reaplica (ops REDOABLE = add_*/set_*/move/delete/dup/update/rename/multi_; ops __ internas nunca). Testado: 51→52 (poste)→51 (undo)→52 (redo)→51 (undo).
- PROPRIEDADES FLUTUANTES: só aparece com seleção (canto sup. direito) — ID, tipo, perfil + kg/m, comprimento, start/end ou centro+dimensões; ações Duplicar/Excluir; fecha no Esc/✕/deselect. Lote (Shift/área): variante compacta com chips de IDs + duplicar/excluir lote.
- DRAWER "Elementos" (botão ☰, temporário, desliza da direita e devolve o espaço ao fechar): seção PROJETO (nome/preset/painel/prof/PD/peso) + grupos MESCLADOS por rótulo amigável (Gabinetes, Gaiola, Postes, Passarela, Guarda-corpo, Chapas, Quadro, Travessas, Diagonais, Treliça, Tirantes, Escada…) com "todos" por grupo; item clica → seleciona no 3D + abre props.
- MODAL NOVO (3 abas): (1) Painel reto — gabinete L/A/P, colunas, linhas, postes, pé-direito, prof. da gaiola, painel resultante calculado ao vivo, checkboxes Gaiola/Passarela/Guarda-corpo, botão CRIAR → monta o "PADRÃO DE PEDIDO" e envia à IA real (/api/ai/command) → fecha e mostra só o projeto; E2E REAL: GLM desenhou 32 elems (GAIOLA FRENTE/TRAS, POSTES, PASSARELA PISO, GUARDA-CORPO, CHAPAS) em ~12 s. (2) Cubo — lado + perfil. (3) Presets salvos — ⬚ Em branco + todos os presets (custom com exclusão em 2 passos).
- MODAL IMPORTAR JSON: textarea (colar) + arquivo opcional + IMPORTAR (validação no Python). MODAL IA: chat real (bolhas user/IA/erro, tags engine GLM/ferramenta, chips de 1 clique, placeholder livre) com ⚙ conector (z.ai/Gemini/OpenAI/compatível + 🔌 teste real). AJUDA (?) simplificada: atalhos + interação + padrão de pedido IA.
- PRANCHA FINAL (estado separado GERAR PROJETO): fullscreen dark chrome + "papel" branco — título/data, meta chips (painel/prof/PD/instalação/elementos/massa), 6 vistas SVG COM COTAS (FRONTAL/TRASEIRA/LATERAL/SUPERIOR/INFERIOR/ISOMÉTRICA), LISTA DE MATERIAIS sem preço (pos/descrição/tipo/compr/unid/quant/pesos + total 507,5 kg), NOTAS (esboço paramétrico + ⚠ não substitui cálculo/ART) e exports discretos (CSV/DXF/DXF.zip/JSON/GLTF/Pacote/Plano de corte). Botões VOLTAR AO EDITOR e ⤓ BAIXAR PDF (/api/export/pdf preservado).
- TEMA ESCURO TÉCNICO: bg #0a0d12, painéis #10141b, linhas #232b38, accent teal (#14b8a6/#2dd4bf), seleção âmbar; cena three.js escura (bg 0x0b0f15, grid sutil, barras cinza-aço claras, painel teal translúcido, eixos vermelho/verde).
- FIXES no caminho: body flex-column (stage não tinha altura → toolbar cortada/cena vazia), drawer fechado criava 705px de hscroll (overflow:hidden no #stage), label truncado da toolbar, __cad.pick tinha typo `ouse.x` (herdado — corrigido), mobile 390px sem scroll horizontal (header com scroll interno, hint oculta ≤560px).
- E2E agent-browser (rota do usuário :3000 → rewrite → :3100): boot escuro 51 elems; 7 vistas na mesma câmera; seleção→props→Esc; drawer abre/fecha/agrupa; Novo 3 abas + presets reais; Importar; IA chat + conector 4 provedores; Ajuda; PRANCHA 6 frames c/ cotas + materiais + notas + VOLTAR; Barra popover 16 perfis; mutações controladas (Poste/Dup/Del/Undo/Redo) com backup prévio e restauração; CRIAR via GLM real; node --check OK; server.log sem erros; sessão final = 51 elems canônicos (backup _session.backup-51elems.json intacto).

Stage Summary:
- O produto agora se comporta como Paint + SketchUp simplificado: UMA grande área de desenho (80–90%), zero painéis permanentes, tudo sob demanda (props só com seleção, drawer temporário, modais para Novo/Importar/IA, prancha só no GERAR PROJETO).
- Dois estados nítidos: EDITOR ↔ PRANCHA FINAL — nunca misturados.
- Motor Python/JSON/API/presets/PDF/exportações 100% preservados (nenhum .py tocado); Refazer adicionado sem backend (stack client-side).
- Arquivo alterado: mini-services/led-cad/static/index.html (reescrito). Dados: _session.json restaurado do backup canônico.
- Próximos passos sugeridos: snapshots PNG da cena dentro da prancha (perspectiva real além da isométrica SVG), carregar/salvar projetos acessível pelo drawer (hoje só via Importar), duplex/trocar perfil no painel flutuante, e modo "medir" com cotas dinâmicas na cena.

---
Task ID: 7
Agent: Z.ai Code (principal)
Task: EVOLUÇÃO — EDITOR CAD REAL + AI COPILOT OPERACIONAL (pedido explícito). NÃO alterar motor geométrico / schema / backend existente / Python como motor. Novas camadas ADITIVAS: (1) ferramentas CAD semânticas completas, (2) snap+ORTHO visuais, (3) inspector com edição numérica, (4) copiloto IA dockado que devolve OPERAÇÕES com PREVIEW (APLICAR/CANCELAR), (5) histórico unificado manual+IA.

Work Log:
- CAMADA PYTHON NOVA (core/ai_ops.py, ADITIVA — motor intocado): normalize_op (aceita formato do usuário {"type","delta":{x,y,z}} E canônico {"operation","delta_x"}); handlers NOVOS semânticos: add_element, add_diagonal (com from_element/to_element — liga extremidades mais próximas), resize_element (barra por length com âncora start/end/center; chapa por size_x/y/z), replace_element (mesmo ID), rotate_element (eixo+ângulo em torno do centro), set_group, set_role, align_elements (eixo+âncora min/center/max pela caixa coletiva), distribute_elements (centros uniformes), mirror_elements (plano ⊥ eixo + pivot + copy) + TODOS os handlers originais importados (move/set_position/add_*/multi_*/set_panel/add_circle/add_box/add_grid/rename/set_shield).
- dry_run_diff(): simula ops em CÓPIA profunda e devolve DIFF {added, removed, changed(before/after), counts, errors} SEM tocar no projeto — alimenta o preview. apply_batch(): aplica o lote com UM snapshot de undo (Ctrl+Z desfaz tudo de uma vez) + ATÔMICO (dry-run antes; se qualquer op é inválida, NADA é aplicado).
- ENDPOINTS NOVOS (main.py): POST /api/ops/preview (diff sem aplicar) · POST /api/ops/apply (lote com 1 undo, action "ia"|"editor") · POST /api/ai/plan (copiloto: utilidades determinísticas executam na hora; resto → LLM com CONTEXTO VIVO devolve APENAS OPERAÇÕES + diff de preview; __select resolvido na UI; NADA é aplicado sem APLICAR).
- ai_prompts.py: build_system_prompt ganhou kwargs view/selection_ids (retrocompatível) + seções novas: SELEÇÃO ATUAL (detalhe completo dos elementos selecionados: id/tipo/papel/perfil/start/end/comprimento/grupo), GRUPOS EXISTENTES, VISTA ATUAL; REGRAMENTO DE OURO no prompt: projeto existente = SEMPRE operações pontuais, NUNCA __blank para pequenas alterações; novos ops documentados; exemplos de EDIÇÃO (duplicar selecionado +960mm, mover V03).
- FRONTEND REESCRITO (static/index.html, tema CLARO — canvas branco, painéis brancos, texto grafite, accent teal #0d9488): TOOLBAR vertical 20 ferramentas em 5 grupos (Selecionar/Mover gizmo/Rotacionar 90°/Copiar→colar no destino/Duplicar/Excluir · Barra/Poste/Chapa/Diagonal · Medir/Cota visual persistente · Alinhar/Distribuir/Paralela/Espelhar/Podar/Esticar · Desfazer/Refazer) — todas SEMÂNTICAS via ops do motor (podar/esticar = set_position com projeção; paralela = duplicate com delta perpendicular pela bitola do perfil); SNAPBAR: snap master + ponta/meio/cruz(cliente, ponto mais próximo entre 2 barras)/aresta/grelha + ORTHO auto/X/Y/Z (constrange A→B ao eixo) + toggle painel; INSPECTOR contextual com EDIÇÃO NUMÉRICA (perfil select, papel, grupo, A/B ou centro+tamanho, comprimento→resize_element, peso calc) e Aplicar em lote (1 undo) + renomear ID; DOCK COPILOTO IA 380px à direita (colapsável; overlay no ≤1100px com FAB) com chips de contexto ao vivo (vista/seleção/projeto), sugestões CONTEXTUAIS de seleção (duplicar/mover/trocar perfil/paralela/diagonal/excluir) ou gerais, e fluxo PLAN→PREVIEW; PREVIEW no canvas: novos translúcidos teal, movidos = original âmbar + fantasma, removidos = vermelho + barra "✨ N alteração(ões) proposta(s) [APLICAR][CANCELAR]" + botões inline no chat; COTAS visuais (linha+ticks+rótulo sprite) por sessão; histórico UNIFICADO (opsStack/redoStack client alinhado com a pilha do servidor; lotes IA = 1 entrada).
- BUGS ACHADOS E CORRIGIDOS: (1) el.length() não existe em objetos JSON do modelo → helper elLen(); (2) HANDLERS sem add_grid/add_box/add_circle/set_panel/multi_*/rename → lote da passarela dava 400 "Operação desconhecida: 'add_grid'" → todos importados; (3) applyOps engolia erro e applyPlan mostrava "✅ Aplicado" mesmo com 400 → erros agora PROPAGAM e a mensagem mostra "Falhou ao aplicar: … — nada foi alterado"; (4) apply_batch não era atômico (push antes de validar) → dry-run de validação ANTES do push (all-or-nothing); (5) FAB da IA aparecia com dock aberto no mobile → setDock controla display + dock inicia fechado no ≤1100px; (6) previewBar colidindo com a viewbar → top:88px.
- INFRA: uvicorn REINICIADO SEM --reload (StatReload crasha no sandbox: OSError /proc/*/net) — start-stop-daemon + pidfile ledcad.pid; usar `kill $(cat ledcad.pid)` + start-stop-daemon para reiniciar após editar .py.
- E2E REAL (agent-browser, rota do usuário :3000/?XTransformPort=3100, 12 screenshots tool-results/t7-*.png): boot claro com 51 elems canônicos (507,53 kg) · seleção POSTE-01 → inspector completo + chips contextuais · "adicione outro poste igual a esse 960 mm para a direita" → GLM: "Vou duplicar POSTE-01 e mover a cópia 960 mm no eixo X." + 1 alteração proposta com FANTASMA translúcido no canvas · APLICAR → 52 elems/640,3 kg/2 postes · Ctrl+Z → 51 · Ctrl+Shift+Z → 52 · Ctrl+Z → 51 · inspector: comprimento 2975→3200 via resize_element · vista FRONTAL + drawer agrupado · prancha final clara com 6 vistas + materiais · modal Novo (3 abas) · passarela via IA: 17 alterações/11 ops com preview translúcido completo (piso+guarda-corpo) → aplicar 68 → undo 51 · /api/ai/plan com seleção responde "O elemento selecionado (V01) utiliza METALON_40x40x2" (contexto vivo funcionando) · SEM erros no console.
- SESSÃO PROTEGIDA: data/_session.json final = BYTE-IDÊNTICO ao backup canônico _session.backup-51elems.json (51 elems, DOOH BLANK, POSTE-01 end z=3000). Drift temporário (3225) durante testes era artefato de restarts do serviço no meio da sessão de teste — restaurado.

Stage Summary:
- O sistema agora é um CAD estrutural real + copiloto operacional: 20 ferramentas semânticas (nunca mesh), snap visual com interseção client-side e ORTHO, inspector com edição numérica, e a IA que EDITA (operações pontuais com preview APLICAR/CANCELAR) em vez de regenerar — exatamente o fluxo IA → operations JSON → Python → modelo → nova geometria → Three.js.
- Histórico único: Ctrl+Z desfaz indiferentemente ops manuais e lotes da IA (1 passo por lote).
- Motor geométrico, schema, presets, PDF/exports e endpoints existentes 100% preservados (main.py só ganhou 3 endpoints novos; core/operations.py intocado).
- Arquivos: core/ai_ops.py (NOVO), main.py (+3 endpoints), services/ai_prompts.py (contexto vivo + regra operations-first), static/index.html (reescrito tema claro).
- Próximos passos sugeridos: streaming da resposta do copiloto; dimensões persistidas (exigiria schema novo — hoje são visuais); tool "escada/escada de mão" paramétrica; teste de carga com 500+ elementos no preview; snap "interseção" também no servidor.
