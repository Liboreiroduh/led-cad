{
  "name": "led-collor-visualizador",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Apresentação técnica interativa do painel LED em V — LED COLLOR",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^8.3.0"
  }
}
